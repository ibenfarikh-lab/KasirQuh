log V.3.0.2
Perbaikan dan Perubahan
👇

### 🔐 ATURAN WAJIB LOG PROYEK — README.md

Setiap pihak, proses, alat, atau aktivitas yang **mengakses, membaca, memeriksa, mengaudit, menganalisis, mengubah, menambah, menghapus, memindahkan, merapikan, memperbaiki, mengganti, memverifikasi, atau melakukan tindakan apa pun terhadap file/proyek** wajib meninggalkan log yang sesuai di `readme.md`.

Tidak boleh ada perubahan atau tindakan terhadap proyek yang dilakukan tanpa dicatat di `readme.md`.

Setiap log wajib mencantumkan:
1. **Tanggal/Waktu** aktivitas.
2. **TRACE ID** pekerjaan.
3. **SCOPE** file/folder yang menjadi ruang lingkup.
4. **Aktivitas** — BACA / AUDIT / ANALISIS / UBAH / TAMBAH / HAPUS / PINDAH / PERBAIKI / VERIFIKASI / LAINNYA.
5. **File terkait** yang diakses atau diproses.
6. **Tujuan** aktivitas.
7. **Temuan/Hasil** aktivitas.
8. **Perubahan** file jika ada.
9. **Verifikasi** setelah perubahan atau pemeriksaan.
10. **Status** — PASS / FAIL / BLOCKED / NO CHANGE.
11. **Catatan** tambahan bila diperlukan.

Jika aktivitas hanya berupa **baca, audit, pemeriksaan, atau diagnosis tanpa perubahan kode**, aktivitas tersebut tetap wajib dicatat dengan status `NO CHANGE`.

Jika aktivitas menemukan masalah tetapi belum melakukan perbaikan, temuan tetap wajib dicatat.

Jika pekerjaan merupakan kelanjutan dari pekerjaan sebelumnya, gunakan TRACE ID yang sama atau TRACE ID turunan yang jelas hubungannya dengan pekerjaan induk.


### 🛡️ ATURAN SCOPE

Setiap TRACE ID memiliki **SCOPE** yang wajib dihormati. File di luar SCOPE tidak boleh diubah tanpa persetujuan eksplisit. Jika pemeriksaan menemukan keterkaitan dengan file di luar SCOPE, catat sebagai **TEMUAN** saja dan jangan mengubah file tersebut sebelum scope diperluas secara eksplisit.

Untuk TRACE saat ini:
- `pelanggan.html` = target utama.
- CSS yang berkaitan langsung dengan `pelanggan.html` = termasuk scope.
- `readme.md` = wajib diperbarui untuk setiap log aktivitas.
- `admin.html` dan `index.html` = **di luar scope perubahan** kecuali ada keputusan eksplisit.

### 🧾 FORMAT LOG WAJIB

```text
[LOG AKTIVITAS]

Tanggal/Waktu :
TRACE ID      :
SCOPE         :
AKTIVITAS     :
FILE TERKAIT  :
TUJUAN        :
TEMUAN        :
PERUBAHAN     :
VERIFIKASI    :
STATUS        :
CATATAN       :
```

### 🚫 ATURAN RIWAYAT

Riwayat log sebelumnya tidak boleh dihapus atau ditimpa hanya untuk merapikan README. Log baru harus **ditambahkan secara berurutan**, sehingga `readme.md` tetap menjadi sumber histori pekerjaan proyek dan selalu dapat dibandingkan dengan kondisi file aktual.




<!-- KQ-V3.0.2-TRACE-ID: PELANGGAN-CLEANUP
SCOPE AKTIF: pelanggan.html + CSS terkait pelanggan.html
ATURAN: Setiap audit, diagnosis, keputusan, perubahan, perbaikan, penghapusan, penambahan, verifikasi, dan temuan wajib dicatat di bawah marker ini atau pada bagian tahap terkait.
JANGAN memperluas scope ke admin.html/index.html tanpa keputusan eksplisit.
STATUS TRACE: AKTIF

POSISI TRACE: Marker ini ditempatkan tepat setelah ATURAN WAJIB LOG PROYEK agar siapa pun yang membuka README.md langsung mengetahui scope pekerjaan aktif dan batas perubahan yang berlaku.
-->


### 🔖 PENANDA PELACAKAN PERUBAHAN — V3.0.2
**TRACE ID:** `KQ-V3.0.2-PELANGGAN-CLEANUP`
**Scope aktif:** `pelanggan.html` + CSS yang berkaitan langsung dengan proses pelanggan.
**Aturan wajib:** Setiap perubahan, diagnosis, audit, keputusan, penghapusan, penambahan, perbaikan, temuan, dan hasil verifikasi setelah penanda ini **wajib direkam di README ini**.
**Pembatas scope:** `admin.html` dan `index.html` tidak boleh ikut diubah hanya karena ditemukan kandidat cleanup; perlu keputusan eksplisit terlebih dahulu.
**Status penanda:** AKTIF — menjadi referensi histori untuk seluruh pekerjaan berikutnya.

> **LOG BERJALAN:** Tambahkan entri baru di bagian tahap terkait setiap kali ada tindakan atau diagnosis. Jangan mengandalkan percakapan sebagai satu-satunya histori.

### LOG AKTIVITAS — PEMBARUAN ATURAN README — 2026-09-23

[LOG AKTIVITAS]

Tanggal/Waktu : 2026-09-23
TRACE ID      : `KQ-V3.0.2-PELANGGAN-CLEANUP`
SCOPE         : `pelanggan.html` + CSS terkait + `readme.md`
AKTIVITAS     : UBAH / DOKUMENTASI
FILE TERKAIT  : `readme.md`
TUJUAN        : Menetapkan aturan wajib agar setiap aktivitas terhadap proyek meninggalkan jejak log yang konsisten dan agar scope pekerjaan tidak melebar tanpa persetujuan.
TEMUAN        : Aturan logging sebelumnya sudah memiliki TRACE ID dan scope, tetapi belum memuat format log wajib yang lengkap serta aturan eksplisit untuk aktivitas baca/audit tanpa perubahan dan pembatasan scope.
PERUBAHAN     : Menambahkan aturan wajib log proyek, aturan scope, format log standar, serta aturan perlindungan riwayat README.
VERIFIKASI    : Isi `readme.md` diperiksa kembali setelah penambahan aturan.
STATUS        : PASS
CATATAN       : Aturan ini berlaku sebagai pagar kerja untuk tahap berikutnya. Tidak ada perubahan pada kode aplikasi.


## AUDIT PEMBERSIHAN — pelanggan.html / CSS

**Status:** Audit read-only, belum ada perubahan pada kode aplikasi.

### Audit CSS — 2026-09-23
- Diperiksa `pelanggan.html` dari `KasirQuh-main-V3-02.zip`.
- Ditemukan **47 blok `<style>`**.
- Ditemukan **683 CSS rules** dan **670 rules unik** berdasarkan pasangan selector + body.
- Terdapat **13 rule duplikat identik**; sebagian merupakan duplikasi dalam blok yang sama dan sebagian lintas blok.
- Duplikasi lintas blok yang perlu diverifikasi sebelum penghapusan:
  - `.category-chips .chip-btn.active` — blok 3.
  - keyframe `0%/5%/10%/15%/20%/25%/30%/37.5%/100%` — blok 1 dan 3.
  - `.product-catalog-grid .kq-platform-card .kq-product-info` — blok 24.
  - `body[data-theme="modern"] #cartModal .cart-table tbody tr` — blok 33 dan 47.
  - `#recipe-scroll-container.horizontal-scroll-container > .recipe-card .recipe-btn-action` — blok 41 dan 42.
- Duplikasi dalam blok yang sama juga ditemukan pada `.product-catalog-grid`, `100%`, dan beberapa selector keyframe.

### Keputusan audit
- **Belum menghapus CSS apa pun.**
- CSS yang tampak duplikat tidak otomatis dianggap aman dihapus karena urutan blok dan override dapat memengaruhi tampilan.
- Tahap berikutnya adalah mencocokkan selector kandidat dengan pemakaian HTML/JS dan urutan cascade sebelum menentukan `hapus / pertahankan / gabungkan / refactor`.
- Perubahan kode hanya dilakukan setelah hasil audit disepakati dan setiap perubahan dicatat di README ini.

### Aturan kerja pembersihan
1. Audit read-only terlebih dahulu.
2. Tidak menghapus selector hanya berdasarkan nama blok atau label versi.
3. Setiap perubahan wajib dicatat di README.
4. Setelah perubahan, lakukan verifikasi dan catat hasil PASS/FAIL.
5. ZIP hasil perubahan hanya dibuat setelah perubahan benar-benar dieksekusi dan diverifikasi.


## AUDIT PEMAKAIAN SELECTOR CSS — pelanggan.html / HTML + JS

**Tanggal:** 2026-09-23  
**Status:** Read-only audit. **Belum ada selector CSS yang dihapus atau dipindahkan.**

### Metode
- Selector CSS dari seluruh **47 blok `<style>`** pada `pelanggan.html` dicocokkan terhadap markup HTML dan sumber JavaScript dalam project.
- Pemeriksaan difokuskan pada class, ID, atribut selector, serta keberadaan string selector di HTML/JS untuk menangkap penggunaan statis maupun indikasi penggunaan dinamis.
- Selector yang hanya muncul di CSS dicatat sebagai **kandidat legacy/unused**, bukan langsung dianggap aman untuk dihapus.

### Hasil utama
- Ditemukan **15 kandidat selector yang tidak ditemukan pemakaiannya di HTML/JS** setelah CSS block dikeluarkan dari pencarian.
- Kandidat tersebut terkonsentrasi pada CSS promo/customer-home lama dan satu selector logo tema gelap.
- Tidak ditemukan pemakaian `customerPromo`, `promoTrigger`, `customer-promo`, `promo-trigger`, `customer-running-wrap`, atau `customer-home-logo-dark` di markup/JS aktif selain definisi CSS-nya.
- Kelompok `.promo-*` berikut juga hanya ditemukan sebagai definisi CSS dan perlu verifikasi sebelum penghapusan karena dapat berkaitan dengan generator/template lama: `promo-mode-badge`, `promo-surprise`, `promo-product-img`, `promo-product-name`, `promo-price-label`, `promo-new-price`, `promo-save`, `promo-stock`.

### Kandidat selector legacy/unused
1. `.promo-mode-badge`
2. `.promo-surprise`
3. `.promo-product-img`
4. `.promo-product-name`
5. `.promo-price-label`
6. `.promo-new-price`
7. `.promo-save`
8. `.promo-stock`
9. `.customer-running-wrap`
10. `.customer-promo`
11. `.customerPromo`
12. `.promo-trigger`
13. `#customerPromo`
14. `#promoTrigger`
15. `.customer-home-logo-dark`

### Catatan penting
- **Belum ada penghapusan.** Audit pemakaian selector saja belum cukup untuk menyatakan CSS aman dihapus.
- Sebelum eksekusi, kandidat harus dicek terhadap kemungkinan pembentukan class/ID secara dinamis melalui concatenation/template, serta hubungan dengan fitur promo yang masih aktif.
- Selector yang terbukti legacy setelah verifikasi dapat masuk tahap **cleanup CSS** berikutnya dan wajib dicatat lagi di README sebagai perubahan terpisah.

### Keputusan tahap ini
**PASS — Audit penggunaan selector selesai.**  
**NEXT — Verifikasi kandidat legacy/unused + audit cascade/override, lalu baru menentukan selector yang benar-benar aman untuk dihapus atau digabung.**


## AUDIT CASCADE / OVERRIDE — pelanggan.html / CSS

**Tanggal:** 2026-09-23  
**Status:** Read-only audit. **Belum ada CSS yang dihapus, digabung, atau dipindahkan.**

### Hasil
- Pemeriksaan urutan **47 blok `<style>`** menunjukkan **64 selector berulang** (tidak termasuk percentage keyframe).
- Dari selector berulang tersebut, terdapat **137 selector** yang memiliki deklarasi berbeda dan karena itu merupakan kandidat cascade/override, bukan duplikasi otomatis.
- Contoh override yang perlu dipertahankan sampai ada verifikasi visual/fitur: `.customer-home-title`, `.customer-home-greeting`, `.promo-banner`, `.product-catalog-grid`, `.btn-quick-cart-icon`, `.reorder-card`, `.inv-card`, `body[data-theme="modern"] .modern-card-price`, serta beberapa selector header/home.
- Beberapa selector berulang berasal dari blok akhir yang tampaknya berfungsi sebagai patch/penyesuaian tema; penghapusan berdasarkan selector saja berisiko menghilangkan aturan terakhir yang sengaja menimpa aturan sebelumnya.

### Verifikasi kandidat legacy sebelumnya
- Pencarian diperluas ke file HTML/JS project, bukan hanya `pelanggan.html`.
- Kandidat berikut **belum ditemukan sebagai pemakaian non-CSS** pada pemeriksaan ini: `.promo-mode-badge`, `.promo-surprise`, `.promo-product-img`, `.promo-product-name`, `.promo-price-label`, `.promo-new-price`, `.promo-save`, `.promo-stock`, `.customer-running-wrap`, `.customer-promo`, `.customerPromo`, `.promo-trigger`, `#customerPromo`, `#promoTrigger`, `.customer-home-logo-dark`.
- Statusnya tetap **kandidat legacy**, bukan otomatis aman dihapus, karena pemakaian dapat muncul melalui markup/template yang dibangun dari sumber eksternal atau string yang tidak tertangkap pencarian sederhana.

### Keputusan
- **Tidak ada penghapusan pada tahap ini.**
- Selector berulang dengan deklarasi berbeda diperlakukan sebagai **override aktif sampai terbukti sebaliknya**.
- Tahap cleanup berikutnya harus memprioritaskan selector yang benar-benar redundant/identik, lalu verifikasi tampilan pada fitur terkait sebelum dan sesudah perubahan.

**PASS — Audit cascade/override selesai.**
**NEXT — Audit HTML structure/UI dependency pada `pelanggan.html`, kemudian baru menyusun daftar perubahan CSS yang aman dieksekusi.**

## AUDIT STRUKTUR HTML/UI — pelanggan.html

**Tanggal:** 2026-09-23  
**Status:** Read-only audit. **Belum ada perubahan HTML/UI.**

### Ringkasan struktur
- `pelanggan.html`: sekitar **6.419 baris** dan **459 elemen HTML** yang ter-parse.
- **162 ID**, semuanya unik; tidak ditemukan duplicate ID.
- Struktur utama berada di `.main-content` dengan 6 bagian langsung: sticky search, promo modal, `belanja`, `data-pelanggan`, `live-chat`, dan `pengaturan`.
- Tidak ditemukan elemen `<main>`, `<header>`, `<footer>`, `<nav>`, atau `<section>` pada markup saat ini. Ini bukan otomatis kesalahan karena layout memakai `<div>`, tetapi menjadi catatan semantik/accessibility.
- Terdapat **8 `<script>`**, termasuk Firebase compat dan beberapa patch/utility inline.
- Terdapat **18 elemen yang terindikasi modal/overlay**; struktur modal secara umum masih dapat dipetakan dan tidak menunjukkan duplicate ID.

### Temuan yang perlu diperhatikan
1. **Inline style sangat banyak:** sekitar **230 atribut `style`**. Ini merupakan kandidat refactor jangka lanjut, tetapi tidak boleh dipindahkan ke CSS sekaligus karena kemungkinan dipakai untuk state UI dinamis.
2. **Inline event handler:** audit sebelumnya menemukan **81 handler** (`onclick`, `oninput`, `onsubmit`, dll.). Ini merupakan coupling HTML → JS yang cukup tinggi. Tidak direfactor pada tahap ini.
3. **43 tombol tidak memiliki atribut `type`.** Namun dua form utama memiliki tombol submit yang eksplisit. Tombol lain perlu audit konteks satu per satu sebelum diberi `type="button"`; jangan melakukan mass-replace karena dapat mengubah perilaku form.
4. **21 kontrol form tidak memiliki label/ARIA yang terdeteksi secara statis.** Banyak di antaranya memakai placeholder sebagai petunjuk. Ini menjadi kandidat perbaikan accessibility, bukan cleanup kosmetik.
5. **1 gambar tidak memiliki `alt`:** `#detail-img`. `#qr-code-img` sudah memiliki `alt="QR Code"`.
6. Tidak ditemukan duplicate ID pada struktur HTML.
7. Beberapa ID tidak ditemukan pada referensi JavaScript statis. Sebagian besar merupakan ID elemen CSS `<style>/<script>` atau elemen yang mungkin hanya menjadi target HTML/fitur tertentu. Karena itu **tidak boleh dianggap unused hanya dari pemeriksaan statis**.
8. Tidak ditemukan indikasi kuat bahwa modal atau tab utama boleh dihapus. `belanja`, `data-pelanggan`, `live-chat`, dan `pengaturan` merupakan bagian struktural aktif.

### Catatan struktur
- `.main-content` masih menjadi container utama dan sebaiknya dipertahankan.
- Sistem modal tersebar tetapi memiliki pola yang cukup jelas (`modal-backdrop`, `modal-content`, `btn-close-modal`). Refactor penyatuan markup sebaiknya dilakukan hanya setelah dependency JS dan CSS masing-masing modal selesai diaudit.
- Banyak komponen memakai ID langsung dari JavaScript; perubahan nama ID dapat memutus fungsi.
- Tidak ada perubahan terhadap ID, class, nesting, atau event handler pada tahap audit ini.

### Prioritas kandidat perbaikan
**H-01** — Audit tombol tanpa `type`, khususnya yang berada dekat/di dalam form.  
**H-02** — Tambahkan `alt` yang tepat pada `#detail-img`.  
**H-03** — Audit accessibility 21 kontrol form yang belum memiliki label/ARIA.  
**H-04** — Audit 230 inline style untuk menentukan mana state dinamis dan mana yang dapat dipindahkan ke CSS.  
**H-05** — Audit 81 inline event handler sebelum mempertimbangkan event delegation/refactor.

### Keputusan tahap ini
**PASS — Audit struktur HTML/UI selesai.**  
**Tidak ada HTML/UI yang dihapus, dipindahkan, atau diubah.**  
**README wajib diperbarui lagi pada setiap eksekusi H-01 sampai H-05.**

**NEXT — Audit JavaScript utility/legacy dan dependency HTML ↔ JS sebelum mulai cleanup yang bersifat destruktif.**


## Audit JavaScript Utility / Legacy + Dependency HTML ↔ JS — 2026-09-23

Status: PASS — audit read-only, belum ada perubahan kode aplikasi.

### Temuan
- `pelanggan.html` memiliki 8 blok `<script>` inline dengan sekitar 146 ribu karakter JavaScript.
- 129 deklarasi fungsi ditemukan, dengan 128 nama fungsi unik.
- Ditemukan 1 nama fungsi ganda: `klaimKoinHarian()` (2 implementasi berbeda). Keduanya belum dihapus karena implementasinya berbeda dan perlu keputusan/tes perilaku sebelum cleanup.
- 7 fungsi yang pada pemeriksaan sederhana tampak hanya memiliki deklarasi ternyata tetap memiliki pemakaian tidak langsung melalui event/listener/IIFE; tidak dikategorikan sebagai dead code.
- Firebase menggunakan SDK compat 10.8.0 untuk App + Firestore. Tidak ditemukan pemakaian `firebase.auth` pada `pelanggan.html`; ini dicatat sebagai dependency/auth-context check, bukan error otomatis.
- Routing data utama menggunakan `storeCollection()` sebanyak 36 pemakaian; `ACTIVE_TOKO_ID` masih hard-coded `toko_v13` dan tidak diubah pada audit ini.
- `db.collection()` langsung masih muncul di 2 titik dan perlu audit lanjutan untuk memastikan tidak melewati routing toko secara tidak sengaja.
- `localStorage` digunakan sekitar 30 kali dan `sessionStorage` 4 kali; belum ada penghapusan karena sebagian merupakan state/UI persistence.
- `history.*` digunakan secara ekstensif dan tetap dipertahankan karena terkait Android Back/history state.
- Tidak ditemukan `eval()`, `import`, atau `require()` pada inline JS.
- `fetch()` ditemukan 3 kali; dependency endpoint perlu diverifikasi pada audit dependency eksternal.

### Kandidat tindak lanjut
- JS-01: audit dua implementasi `klaimKoinHarian()` dan tentukan implementasi canonical.
- JS-02: audit 2 pemakaian langsung `db.collection()` terhadap aturan `storeCollection()`.
- JS-03: audit seluruh `localStorage/sessionStorage` untuk membedakan state aktif vs legacy.
- JS-04: audit endpoint `fetch()` dan file API terkait.
- JS-05: audit dependency eksternal Firebase/manifest/script terhadap file yang benar-benar digunakan.

### Keputusan
Tidak ada fungsi JavaScript, dependency, atau kode aplikasi yang dihapus/diubah pada tahap ini. Temuan di atas menjadi dasar audit dependency berikutnya.


## Audit Dependency Eksternal + API — 2026-09-23

Status: **PASS — audit read-only, belum ada perubahan kode aplikasi.**

Temuan utama:
- `pelanggan.html` memuat Firebase Compat 10.8.0 (`firebase-app-compat.js` dan `firebase-firestore-compat.js`).
- `admin.html` memuat Firebase Compat 10.8.0 untuk app, Firestore, dan Auth; juga `html5-qrcode` dari unpkg.
- `pelanggan.html` memakai `/api/tts` dan `/api/tanya`; `admin.html` memakai `/api/admin-ai`.
- `index.html` juga memakai `/api/tts` dan `/api/tanya`.
- `api/tanya.js` dan `api/admin-ai.js` sama-sama menggunakan `GROQ_API_KEY` dan endpoint Groq dengan model `openai/gpt-oss-20b`.
- `package.json` mendeklarasikan `node-edge-tts` dan `@google/genai`; audit awal belum menemukan penggunaan `@google/genai` pada file yang diperiksa, sehingga menjadi kandidat dependency review, bukan langsung dihapus.
- `api/tts.js` menggunakan `node-edge-tts`, sehingga dependency tersebut aktif/terpakai.
- `api/admin-ai.js` memiliki struktur yang sangat mirip dengan `api/tanya.js`, tetapi endpoint frontend berbeda; jangan digabung sebelum kontrak pemanggilan diverifikasi.
- `vercel.json` hanya mengatur Cache-Control untuk file HTML.
- `sw.js` melakukan cache app shell dan secara eksplisit melewati request Firebase. `manifest-pelanggan.json` belum termasuk daftar cache service worker; dicatat sebagai kandidat review PWA, bukan bug pasti.
- External runtime dependencies yang terdeteksi: Firebase CDN, `html5-qrcode` CDN, QR Server API, WhatsApp links, dan Groq API melalui server-side endpoint.

Kandidat tindak lanjut:
- DEP-01: verifikasi apakah `@google/genai` benar-benar tidak dipakai sebelum menghapus dependency.
- DEP-02: audit kontrak `api/tanya.js` vs `api/admin-ai.js` sebelum refactor/gabung.
- DEP-03: review cache `manifest-pelanggan.json` pada `sw.js`.
- DEP-04: audit ketergantungan CDN (`Firebase`, `html5-qrcode`) dan risiko versi/availability; jangan mengganti versi tanpa pengujian.
- DEP-05: audit endpoint QR Server dan WhatsApp sebagai layanan eksternal, termasuk fallback bila layanan tidak tersedia.

Keputusan: **tidak ada dependency, API, CDN, atau service-worker rule yang dihapus/diubah pada tahap ini.**


## Eksekusi DEP-01 — Cleanup Dependency Tidak Terpakai — 2026-09-23
Status: PASS — perubahan pertama dieksekusi setelah verifikasi read-only.

- Verifikasi seluruh source project menunjukkan `@google/genai` hanya muncul sebagai deklarasi dependency di `package.json`; tidak ada import, require, atau pemakaian API `GoogleGenAI` pada source yang diaudit.
- `node-edge-tts` terbukti dipakai oleh `api/tts.js`, sehingga dipertahankan.
- Perubahan yang dilakukan: menghapus dependency `@google/genai` dari `package.json`.
- Tidak ada perubahan pada Firebase, API endpoint, UI, CSS, service worker, atau file aplikasi lain.
- Verifikasi pasca-perubahan: `package.json` valid JSON; `node-edge-tts` tetap terdaftar dan digunakan.

### File berubah
- `package.json`
- `readme.md`

### Catatan
Cleanup berikutnya tetap menunggu audit/keputusan masing-masing. Tidak dilakukan penghapusan file atau refactor massal.

## Audit DEP-02 — Kontrak API tanya.js ↔ admin-ai.js — 2026-09-23
Status: PASS — audit read-only, tidak ada perubahan perilaku API.

Temuan terverifikasi:
- `api/tanya.js` dipanggil oleh `pelanggan.html` melalui `POST /api/tanya` dengan payload `prompt`, `daftarProduk`, dan `namaToko`.
- `api/admin-ai.js` dipanggil oleh `admin.html` melalui `POST /api/admin-ai` dengan payload `prompt`, `daftarProduk`, dan `namaToko`.
- Kedua endpoint menerima POST, membaca `GROQ_API_KEY`, memakai endpoint Groq OpenAI-compatible chat completions, model `openai/gpt-oss-20b`, temperature `0.9`, dan `max_tokens: 500`.
- Keduanya mengembalikan kontrak utama `{ reply: ... }`, sehingga caller masing-masing kompatibel.
- Perbedaan utama berada pada `systemPrompt`: `tanya.js` berorientasi Asisten Belanja pelanggan, sedangkan `admin-ai.js` memiliki prompt yang berbeda/lebih ringkas. Perbedaan ini dipertahankan karena kedua endpoint memang dipanggil dari permukaan UI yang berbeda.
- Tidak ditemukan perbedaan kode di luar isi `systemPrompt` dan komentar header yang mengharuskan refactor untuk menjaga kontrak frontend.
- Komentar header `api/admin-ai.js` masih menyebut `// api/tanya.js`; dicatat sebagai kandidat cleanup dokumentasi/kode minor, tetapi tidak diubah pada tahap ini agar audit tidak bercampur dengan perubahan perilaku.

Keputusan tahap:
- Tidak menggabungkan kedua endpoint.
- Tidak menghapus salah satu endpoint.
- Tidak mengubah prompt AI.
- DEP-02 dinyatakan PASS dan siap lanjut ke DEP-03 (audit Service Worker + manifest PWA).


## Eksekusi DEP-03 — Service Worker + Manifest PWA — 2026-09-23
Status: **PASS — perbaikan kecil dieksekusi dan diverifikasi.**

Temuan terverifikasi:
- `pelanggan.html` menggunakan `manifest-pelanggan.json` dan mendaftarkan `/sw.js`.
- `index.html` dan `admin.html` menggunakan `manifest.json`.
- Sebelum perbaikan, `sw.js` hanya memasukkan `manifest.json` ke app-shell cache; `manifest-pelanggan.json` belum ikut dicache.
- Navigasi ke `/pelanggan.html` tetap memiliki fallback offline, tetapi manifest pelanggan sendiri dapat bergantung pada network saat diminta.

Perubahan aman yang dilakukan:
- Cache version dinaikkan dari `kasirquh-v6` menjadi `kasirquh-v7` agar service worker baru menggantikan cache lama.
- Menambahkan `/manifest-pelanggan.json` ke `urlsToCache`.
- Menambahkan `manifest-pelanggan.json` ke pola static-asset cache-first.
- Tidak mengubah strategi Firebase/network bypass, navigation fallback, atau logic aplikasi pelanggan/admin.

### File berubah
- `sw.js`
- `readme.md`

### Verifikasi
- `sw.js` berhasil melewati pemeriksaan syntax JavaScript.
- Kedua manifest tetap valid JSON.
- `pelanggan.html` tetap menunjuk ke `manifest-pelanggan.json`.
- ZIP hasil tahap ini dibuat dan diverifikasi dapat dibaca tanpa error.

Keputusan: **DEP-03 PASS. Perubahan hanya memperlengkap cache PWA pelanggan; tidak ada refactor service worker.**

## Audit DEP-04 — CDN Firebase + html5-qrcode — 2026-09-23
Status: **PASS — audit selesai, tidak ada perubahan runtime.**

Temuan terverifikasi:
- `pelanggan.html` memakai Firebase compat `10.8.0` untuk `firebase-app` dan `firebase-firestore`.
- `admin.html` memakai Firebase compat `10.8.0` untuk `firebase-app`, `firebase-firestore`, dan `firebase-auth`.
- Versi Firebase dipatok eksplisit ke `10.8.0`, sehingga tidak mengikuti versi CDN terbaru secara otomatis.
- `admin.html` memuat `html5-qrcode` melalui `https://unpkg.com/html5-qrcode` tanpa nomor versi eksplisit.
- `script.js` benar-benar menggunakan `Html5Qrcode` untuk scanner POS dan scanner DB, sehingga dependency tersebut bukan dependency mati.
- Tidak ditemukan salinan lokal `html5-qrcode` pada source yang diaudit; memindahkannya menjadi lokal memerlukan pengujian khusus agar fungsi scanner tidak berubah.
- Tidak ada dasar yang cukup untuk mengganti versi Firebase atau html5-qrcode pada tahap ini. Mengganti versi tanpa regression test dapat mengubah perilaku scanner/auth/firestore.

Keputusan tahap:
- Firebase `10.8.0` dipertahankan.
- `html5-qrcode` dipertahankan dari CDN untuk saat ini.
- Tidak menambahkan SRI atau memindahkan library ke lokal pada tahap ini karena keduanya merupakan perubahan deployment/runtime yang perlu pengujian tambahan.
- DEP-04 dinyatakan PASS dan tidak memerlukan perubahan kode aplikasi.

Catatan kandidat lanjutan:
- DEP-04-A: pertimbangkan pin versi `html5-qrcode` setelah versi yang sedang kompatibel dengan scanner V3 ditentukan dan diuji.
- DEP-04-B: pertimbangkan local bundling/SRI hanya jika workflow deployment dan browser target sudah diuji.


## Audit DEP-05 — QR Server + WhatsApp / External Service — 2026-09-23
Status: **PASS — audit selesai, tidak ada perubahan runtime.**

Temuan terverifikasi pada `pelanggan.html`:
- QR Code ditampilkan melalui `https://api.qrserver.com/v1/create-qr-code/` pada fungsi `bukaModalQRCode()`. URL tujuan di-encode dengan `encodeURIComponent()`.
- Fitur unduh barcode/QR juga mengambil gambar dari QR Server menggunakan `Image()` dengan `crossOrigin = "anonymous"`, lalu menggambarnya ke canvas.
- Berbagi aplikasi menggunakan `https://wa.me/?text=...`; teks pesan di-encode dengan `encodeURIComponent()`.
- Pengiriman pesanan menggunakan `https://wa.me/<phoneToko>?text=...` setelah nomor toko dinormalisasi menjadi format numerik internasional.
- Tidak ditemukan fallback lokal untuk QR Server. Jika layanan QR Server tidak tersedia, fungsi QR eksternal tersebut bergantung pada ketersediaan layanan pihak ketiga.
- Jalur WhatsApp juga merupakan external handoff (`window.open`), sehingga keberhasilannya bergantung pada browser/perangkat dan ketersediaan WhatsApp.
- Tidak ditemukan indikasi bahwa QR Server atau WhatsApp dipanggil otomatis tanpa aksi pengguna pada jalur yang diaudit; pemanggilan berada di fungsi berbasis fitur berbagi/QR/pengiriman pesanan.

Keputusan tahap:
- **Tidak mengganti QR Server** dan tidak menambahkan library QR baru karena perubahan tersebut memerlukan regression test khusus pada fitur cetak/unduh QR.
- **Tidak mengubah URL WhatsApp** karena format yang digunakan sudah melakukan URL encoding dan merupakan bagian dari alur kirim/bagikan yang berjalan.
- Tidak menambahkan fallback palsu yang hanya menyembunyikan kegagalan layanan.
- DEP-05 dinyatakan **PASS** tanpa perubahan kode aplikasi.

Catatan kandidat lanjutan:
- DEP-05-A: tambahkan pesan fallback UI ketika QR Server gagal dimuat.
- DEP-05-B: pertimbangkan generator QR lokal jika nantinya ingin menghilangkan ketergantungan QR Server; wajib regression test download/canvas.
- DEP-05-C: uji handoff WhatsApp pada Android/iOS/desktop sebelum mengubah format URL.

### Rekap DEP-01 s/d DEP-05
- DEP-01 `package.json`: `@google/genai` yang tidak digunakan telah dihapus; `node-edge-tts` dipertahankan.
- DEP-02 API customer/admin: kontrak frontend-backend kompatibel; tidak digabung.
- DEP-03 PWA: `manifest-pelanggan.json` ditambahkan ke cache Service Worker dan cache version dinaikkan.
- DEP-04 CDN: Firebase `10.8.0` dan `html5-qrcode` dipertahankan setelah verifikasi penggunaan.
- DEP-05 external service: QR Server dan WhatsApp diverifikasi; tidak ada perubahan runtime karena tidak ada perubahan yang aman dan wajib pada tahap ini.

**Status rangkaian dependency/API audit: SELESAI.** Seluruh perubahan yang benar-benar dilakukan tetap tercatat di `readme.md`.


## H-02 — Aksesibilitas gambar detail produk — 2026-09-23
Status: **PASS — perbaikan kecil dieksekusi dan diverifikasi.**

Temuan dari audit HTML/UI sebelumnya:
- Elemen `#detail-img` pada modal detail produk tidak memiliki atribut `alt`.
- Elemen tersebut merupakan gambar produk yang digunakan pada UI pelanggan.

Perubahan:
- Menambahkan `alt="Gambar produk"` pada `#detail-img`.
- Tidak mengubah `id`, `src`, style, event handler, atau alur pengisian gambar oleh JavaScript.

File berubah:
- `pelanggan.html`
- `readme.md`

Verifikasi:
- Tepat satu elemen `#detail-img` ditemukan dan berhasil diperbarui.
- Struktur atribut tetap valid secara tekstual dan tidak mengubah JavaScript yang mengakses `#detail-img`.
- Pemeriksaan JavaScript statis tidak menunjukkan perubahan pada logic aplikasi.

Keputusan: **H-02 PASS.** Perubahan dipertahankan sebagai cleanup aksesibilitas berisiko rendah.


### H-01 — Button type audit / cleanup
- Audit menemukan 80 elemen `<button>` di `pelanggan.html`; 59 tidak memiliki atribut `type`.
- Verifikasi konteks menemukan hanya 2 `<form>`, dan masing-masing tombol submit sudah eksplisit `type="submit"`.
- Eksekusi aman: 59 tombol non-submit diberi `type="button"`.
- Tujuan: memperjelas semantik tombol dan mencegah perubahan perilaku menjadi submit bila struktur/form di masa depan berkembang.
- Tidak mengubah handler `onclick`, style, ID, atau alur bisnis.
- Verifikasi pasca-perubahan: 0 tombol tanpa `type`; 2 tombol `type="submit"` tetap dipertahankan.
- Status: **PASS**.


## JS-01 — Cleanup duplicate `klaimKoinHarian()`

**Status: EXECUTED / PASS**

### Audit
- Ditemukan 2 deklarasi fungsi dengan nama identik `klaimKoinHarian()` pada `pelanggan.html` (sekitar baris 3424–3425).
- Karena deklarasi fungsi berada pada scope yang sama, deklarasi kedua menimpa deklarasi pertama saat runtime.
- Implementasi pertama adalah versi yang lebih lengkap: membaca `admin_koin_warga_daily`, memiliki fallback 10 koin, serta terintegrasi dengan state/history modal melalui `history.back()`.
- Implementasi kedua adalah versi legacy yang selalu menambahkan 10 koin dan hanya menutup modal secara langsung.
- Pemanggilan aktif ditemukan pada tombol check-in harian sekitar baris 2523 dan tetap memakai nama fungsi yang sama.

### Perubahan
- Menghapus **hanya deklarasi kedua/legacy** `klaimKoinHarian()`.
- Implementasi pertama dipertahankan sebagai satu-satunya implementasi aktif.
- Tidak mengubah pemanggilan fungsi, storage key, UI modal, atau fungsi lain.

### Verifikasi
- Jumlah deklarasi `klaimKoinHarian()` setelah cleanup: **1**.
- Pemanggilan tombol check-in tetap ada: **PASS**.
- `admin_koin_warga_daily` tetap digunakan oleh implementasi aktif: **PASS**.
- Tidak ada deklarasi fungsi identik kedua: **PASS**.
- Syntax check `pelanggan.html`: **PASS**.

### Catatan
Cleanup ini menghilangkan override legacy yang sebelumnya membuat implementasi lengkap tidak menjadi implementasi efektif saat runtime.


## Audit JS-02 — Direct `db.collection()` vs `storeCollection()` — 2026-09-23
Status: **PASS — audit read-only, tidak ada perubahan kode.**

Temuan terverifikasi:
- Hanya ada dua pemakaian sintaks `db.collection()` di `pelanggan.html`, dan keduanya berada di dalam implementasi `storeCollection(name)` sendiri.
- `db.collection(name)` dipakai sebagai fallback untuk collection yang memang tidak termasuk `V13_STORE_COLLECTIONS`.
- `db.collection("toko").doc(ACTIVE_TOKO_ID).collection(name)` dipakai untuk seluruh collection yang masuk daftar routing toko V13.
- Tidak ditemukan pemanggilan `db.collection("produk")`, `db.collection("pelanggan")`, `db.collection("transaksi")`, atau collection V13 lainnya secara langsung di luar router tersebut.
- Dengan demikian, tidak ada bypass routing `storeCollection()` yang perlu diperbaiki pada tahap ini.

Keputusan tahap:
- Tidak mengubah `storeCollection()`.
- Tidak memindahkan query Firestore.
- Tidak melakukan refactor database karena struktur routing yang ada sudah konsisten dengan aturan toko V13 yang sedang diaudit.
- JS-02 dinyatakan **PASS** dan lanjut ke kandidat berikutnya setelah verifikasi lokal.

## JS-03 — Audit localStorage / sessionStorage — 2026-09-23
Status: **PASS — audit read-only, tidak ada perubahan kode.**

Temuan terverifikasi:
- `pelanggan.html`, `admin.html`, `script.js`, dan `index.html` memiliki pemakaian storage aktif yang terkait state/sesi, preferensi UI, dan fitur aplikasi.
- `sessionStorage` yang ditemukan digunakan untuk state sementara dan memiliki lifecycle yang jelas.
- `localStorage.clear()` hanya ditemukan pada alur pembersihan/reset yang memang dipicu pengguna; tidak diubah.
- Tidak ditemukan key yang cukup terbukti sebagai dead/legacy sehingga aman dihapus tanpa regression test tambahan.

Keputusan:
- Tidak menghapus atau mengganti key storage.
- Tidak melakukan migrasi storage.
- JS-03 dinyatakan **PASS**.

## JS-04 — Audit endpoint, payload, response, dan error handling API — 2026-09-23
Status: **PASS — audit read-only; ditemukan beberapa inkonsistensi yang dicatat sebagai kandidat, tanpa perubahan runtime.**

### 1. Endpoint terverifikasi
- `pelanggan.html` menggunakan `POST /api/tanya` untuk AI pelanggan.
- `index.html` menggunakan `POST /api/tanya` untuk AI pada halaman pembuka.
- `admin.html` menggunakan `POST /api/admin-ai` untuk AI Admin.
- `pelanggan.html` dan `index.html` menggunakan `GET /api/tts?text=...` untuk Text-to-Speech.
- Tidak ditemukan endpoint frontend yang mengarah ke path API yang tidak tersedia di folder `api/`.

### 2. Payload
`/api/tanya` menerima:
- `prompt`
- `daftarProduk`
- `namaToko`

`/api/admin-ai` menerima payload dengan field yang sama.

Server kedua endpoint juga menyediakan fallback kompatibilitas untuk field `pesan`, `message`, dan `text` pada input prompt.

`/api/tts` menggunakan query parameter `text`, sesuai dengan seluruh pemanggil frontend yang diaudit.

### 3. Response
- `/api/tanya` mengembalikan kontrak utama `{ reply }` pada jalur normal maupun fallback.
- `/api/admin-ai` mengembalikan kontrak utama `{ reply }`.
- Frontend pelanggan memiliki fallback tambahan untuk format `candidates[0].content.parts[*].text`, tetapi backend saat ini menormalisasi response menjadi `{ reply }`. Ini bukan bug; fallback hanya kompatibilitas legacy.
- `/api/tts` mengembalikan `audio/mpeg` dan frontend memperlakukannya sebagai Blob/audio URL.

### 4. Error handling — temuan penting
- Kedua endpoint AI menggunakan pola yang sama: sebagian besar error backend dikembalikan sebagai HTTP **200** dengan field `reply`, termasuk API key belum tersedia, rate limit 429 dari provider, dan exception umum.
- Akibatnya, frontend tidak selalu dapat membedakan response sukses dengan error server hanya berdasarkan `response.ok`.
- `admin.html` memang memiliki cabang `response.ok`, tetapi banyak kegagalan backend telah dinormalisasi menjadi HTTP 200 sehingga cabang error HTTP tidak selalu aktif.
- `index.html` dan `pelanggan.html` lebih banyak bergantung pada `try/catch` serta fallback isi `reply`.
- `/api/tts` lebih konvensional: 405 untuk method salah, 400 bila `text` kosong, dan 500 bila pembuatan audio gagal; frontend pelanggan memeriksa `ttsResponse.ok`, sedangkan `index.html` juga melempar error bila response tidak OK.

### 5. Kandidat perbaikan — BELUM DIEKSEKUSI
- JS-04-A: normalisasi status HTTP error pada `/api/tanya` dan `/api/admin-ai`, tanpa mengubah kontrak `{ reply }` yang sukses.
- JS-04-B: tetapkan kontrak error JSON yang konsisten, misalnya `{ error, reply? }`, lalu sesuaikan frontend.
- JS-04-C: audit output AI Admin karena `data.reply` saat ini dimasukkan langsung ke `innerHTML`; perlu dipastikan escaping/sanitization sebelum perubahan.
- JS-04-D: setelah perubahan kontrak error, lakukan regression test untuk pelanggan, halaman pembuka, Admin AI, dan TTS.

### Keputusan tahap
- **Tidak mengubah endpoint, payload, atau response sukses.** Semuanya kompatibel.
- **Tidak mengubah error handling sekarang**, karena perubahan status HTTP berpotensi memerlukan penyesuaian serentak pada beberapa frontend.
- JS-04 dinyatakan **AUDIT PASS / CLEANUP DEFERRED**.

## JS-04-C — Audit keamanan output AI Admin / `innerHTML` — 2026-09-23
Status: **SELESAI — perubahan kecil dan terisolasi.**

Temuan terverifikasi:
- `admin.html` sebelumnya memasukkan `data.reply`, `data.error`, dan `error.message` langsung ke template `innerHTML`.
- Nilai tersebut berasal dari response API atau exception dan tidak perlu diperlakukan sebagai HTML.
- Pola tersebut berpotensi membuat markup dari response dianggap sebagai elemen HTML.

Perubahan:
- Output AI Admin kini dibuat menggunakan `document.createElement('div')` dan `textContent`.
- Response sukses `data.reply` ditampilkan sebagai teks biasa dengan `white-space: pre-line` agar line break tetap terbaca.
- Pesan error HTTP dan pesan exception juga ditampilkan menggunakan `textContent`.
- Tidak mengubah endpoint, payload, kontrak `{ reply }`, prompt, atau alur request.
- Tidak mengubah tampilan loading.

Verifikasi:
- Target `data.reply` tidak lagi dimasukkan langsung ke `innerHTML` pada blok AI Admin yang diaudit.
- `data.error` dan `error.message` pada blok tersebut juga tidak lagi dimasukkan langsung ke `innerHTML`.
- Syntax JavaScript hasil ekstraksi script block: **PASS**.
- ZIP integrity: **PASS**.

Keputusan:
- JS-04-C dinyatakan **PASS**.
- Kandidat berikutnya tetap JS-04-A/B (normalisasi HTTP error), tetapi belum dieksekusi karena membutuhkan regression test frontend + API secara bersamaan.

## JS-05 — Audit Dependency Eksternal / CDN / Layanan Eksternal — 2026-09-23
Status: **PASS — audit read-only, tidak ada perubahan runtime.**

Ruang lingkup:
- Memeriksa seluruh pemanggilan dependency eksternal yang tertanam di HTML/JS/CSS.
- Mencocokkan library eksternal dengan pemakaian aktual di aplikasi.
- Memisahkan dependency library inti dari layanan eksternal yang hanya dipanggil saat fitur digunakan.

Temuan terverifikasi:
- Firebase Compat `10.8.0` dipakai oleh `admin.html` untuk Firebase App, Firestore, dan Auth; `pelanggan.html` memakai Firebase App dan Firestore. Dependency ini **aktif** dan tidak dihapus.
- `html5-qrcode` dari `unpkg.com` dimuat oleh `admin.html` dan terkait fitur scanner QR. Dependency ini **aktif**; versi tidak diubah pada tahap audit.
- `api/tanya.js` dan `api/admin-ai.js` mengakses Groq secara server-side. URL endpoint tidak terekspos sebagai `<script>` CDN dan tidak perlu dibundel ke frontend.
- Fitur pencarian foto pada `script.js` memakai Wikimedia Commons API dan fallback DiceBear secara runtime. Keduanya merupakan dependency fitur, bukan library aplikasi yang aman untuk dihapus.
- Fitur QR pelanggan memakai QR Server secara runtime untuk membuat gambar QR.
- Handoff pesan/pesanan memakai `wa.me` untuk membuka WhatsApp; ini merupakan layanan eksternal berbasis aksi pengguna.
- Tidak ditemukan external stylesheet/font melalui `url(...)` CSS pada audit statis ini.
- Tidak ditemukan external `<script>` atau `<link>` lain selain Firebase dan `html5-qrcode` pada HTML yang diaudit.

Keputusan:
- **Tidak menghapus atau mengganti dependency eksternal apa pun.** Semua dependency yang ditemukan memiliki pemakaian yang dapat ditelusuri ke fitur aktif.
- Tidak melakukan pinning versi `html5-qrcode` karena versi kompatibel harus diuji terhadap scanner V3 terlebih dahulu.
- Tidak melakukan local bundling/SRI Firebase atau `html5-qrcode` pada tahap ini karena perubahan tersebut menyentuh strategi deployment dan wajib regression test.
- Tidak mengubah URL layanan Wikimedia, DiceBear, QR Server, Groq, atau WhatsApp.

Kandidat tahap lanjutan (belum dieksekusi):
- JS-05-A: uji fallback/error UI bila layanan QR Server, Wikimedia, atau DiceBear gagal.
- JS-05-B: regression test scanner setelah kandidat pin versi `html5-qrcode` ditentukan.
- JS-05-C: evaluasi local bundling/SRI hanya jika workflow deployment membutuhkan pengurangan ketergantungan CDN.

Verifikasi:
- Audit pencarian URL eksternal dan `<script>/<link>` eksternal: **PASS**.
- Tidak ada file aplikasi yang diubah selain `readme.md` pada tahap JS-05.
- Tahap ini bersifat read-only terhadap kode aplikasi.

## H-03-A — Selective Accessibility Cleanup
**Status: PASS / APPLIED**

Tanggal audit: 2026-09-23.

Perubahan selektif diterapkan pada kontrol form yang paling sering digunakan dan sebelumnya belum memiliki label aksesibel yang terdeteksi. Ditambahkan `aria-label` pada kontrol login, pencarian, checkout/pembayaran, chat, pengaturan pelanggan, pengaturan Admin, dan input AI.

Tidak dilakukan penambahan label massal pada seluruh kontrol. Kontrol hidden, field impor/file tertentu, dan elemen yang konteksnya belum cukup jelas sengaja tidak disentuh untuk menghindari label yang salah atau perubahan perilaku UI.

File yang berubah:
- `pelanggan.html` — 23 kontrol diberi `aria-label`.
- `admin.html` — 33 kontrol diberi `aria-label`.
- `index.html` — 1 kontrol diberi `aria-label`.
- `readme.md` — catatan H-03-A.

Verifikasi:
- Tidak mengubah endpoint/API, Firebase, routing database, atau logika bisnis.
- Tidak mengubah placeholder, ID, event handler, maupun struktur form.
- Syntax HTML tetap berupa markup valid secara struktural.
- ZIP dibuat tanpa memasukkan ZIP hasil audit sebelumnya (mencegah recursive archive).

Catatan lanjutan: H-04 (inline style audit) dan H-05 (inline event handler audit) tetap menjadi kandidat berikutnya.

## H-04 — Audit Inline Style — 2026-09-23
Status: **AUDIT PASS / CLEANUP SELECTIVE DEFERRED**

Ruang lingkup:
- Menginventarisasi atribut `style="..."` pada `pelanggan.html`, `admin.html`, dan `index.html`.
- Membedakan inline style yang berulang/dapat dipindahkan ke CSS dari style yang kemungkinan bersifat dinamis atau kontekstual.
- Tidak melakukan migrasi massal ke `style.css` pada tahap audit ini.

Temuan terverifikasi:
- `pelanggan.html`: **230** elemen dengan inline style, **185** variasi style unik.
- `admin.html`: **353** elemen dengan inline style, **207** variasi style unik.
- `index.html`: **4** elemen dengan inline style, seluruhnya unik.
- Banyak inline style merupakan kombinasi presentasional yang dipakai berulang, terutama ukuran font, warna tema, display, spacing, dan layout komponen.
- Sebagian inline style menggunakan nilai dinamis atau berada pada elemen UI yang dikontrol JavaScript; pemindahan otomatis berisiko mengubah cascade atau perilaku runtime.

Contoh kandidat yang berulang:
- `pelanggan.html`: `display: none;`, ukuran font, spacing, dan kombinasi warna/teks.
- `admin.html`: `font-size: 0.85rem;`, kombinasi background/border/text tema, `display: none`, serta beberapa pola card/button.

Keputusan:
- **Tidak menghapus inline style hanya karena duplikat.** Duplikasi teks style belum membuktikan bahwa selector/class pengganti memiliki konteks yang sama.
- **Tidak memindahkan seluruh inline style ke `style.css`** pada tahap ini karena berpotensi mengubah specificity/cascade dan style dinamis.
- H-04 dinyatakan **PASS sebagai audit**; cleanup dipisahkan menjadi H-04-A agar setiap migrasi dapat diuji satu kelompok komponen pada satu waktu.

Kandidat H-04-A (belum dieksekusi):
1. Pilih kelompok inline style statis yang identik dan memiliki konteks komponen yang jelas.
2. Buat class CSS pengganti di `style.css`.
3. Ganti hanya kelompok yang sudah diverifikasi tidak bergantung pada inline specificity.
4. Regression check tampilan desktop/mobile dan komponen terkait.
5. Update `readme.md` dan buat ZIP baru tanpa archive hasil audit sebelumnya.

Verifikasi audit:
- Tidak ada perubahan runtime pada tahap H-04.
- Tidak ada CSS/JS/API/Firebase yang diubah.
- H-04 siap dilanjutkan ke cleanup selektif setelah kandidat pertama dipilih.

## H-04-A — Cleanup Inline Style Selektif — 2026-09-23
Status: **PASS — cleanup selektif selesai**

Ruang lingkup:
- Memilih satu kelompok inline style yang statis, identik, dan konteksnya jelas.
- Target: label form di `admin.html` dengan inline style identik `font-size: 0.85rem;`.
- Membuat class CSS bersama `.h04a-form-label` di `style.css`.
- Mengganti **29** label yang tepat sasaran dari inline style menjadi class CSS.

Keamanan perubahan:
- Tidak menyentuh `display:none`, style dinamis, warna tema, layout, atau style yang dikendalikan JavaScript.
- Tidak mengubah ID, event handler, struktur form, Firebase, API, routing, atau logika bisnis.
- Perubahan hanya memindahkan satu deklarasi presentasional statis ke CSS bersama.

Verifikasi:
- `admin.html` memiliki 29 penggunaan `.h04a-form-label` hasil cleanup.
- Tidak ada lagi target inline `font-size: 0.85rem;` pada `admin.html`.
- `style.css` memiliki satu definisi `.h04a-form-label`.
- ZIP tidak menyertakan archive audit sebelumnya.
- H-04-A dinyatakan **PASS**.

Catatan: H-04 belum dianggap selesai seluruhnya; cleanup inline style berikutnya tetap harus dipilih per kelompok dengan prinsip yang sama.

## H-05 — Audit Inline Event Handler — 2026-09-23
Status: **AUDIT PASS / CLEANUP SELECTIVE REQUIRED**

Ruang lingkup:
- Audit atribut event inline pada `index.html`, `pelanggan.html`, dan `admin.html`.
- Memisahkan atribut event HTML yang benar-benar aktif dari pola teks/variabel JavaScript yang kebetulan diawali `on`.
- Tidak memindahkan handler ke JavaScript pada tahap audit.

Temuan terverifikasi dengan parser HTML:
- `index.html`: **0** inline event handler.
- `pelanggan.html`: **81** inline event handler. Rinciannya: `onclick` 69, `onsubmit` 2, `oninput` 1, `onchange` 6, `onkeypress` 3.
- `admin.html`: **148** inline event handler. Rinciannya: `onclick` 122, `onsubmit` 1, `oninput` 8, `onchange` 14, `onkeypress` 2, `onkeydown` 1.
- Total terverifikasi: **229** inline event handler aktif pada HTML.

Catatan penting:
- Angka ini berbeda dari pencarian teks mentah karena pola seperti nama variabel yang mengandung `on...` bukan event HTML dan tidak dihitung.
- Mayoritas handler adalah pemanggilan fungsi UI langsung (`onclick`, `onchange`, `oninput`) yang sudah menjadi bagian dari struktur V3. Memindahkan seluruhnya sekaligus berisiko mengubah urutan inisialisasi, scope fungsi global, atau perilaku UI.
- Handler keyboard yang berisi logika inline (`onkeypress`/`onkeydown`) perlu diperlakukan lebih hati-hati daripada handler pemanggil fungsi sederhana.
- `index.html` tidak memerlukan cleanup H-05 karena tidak memiliki inline event attribute terverifikasi.

Keputusan:
- **Tidak melakukan migrasi massal.**
- H-05 dinyatakan **PASS sebagai audit**.
- Cleanup dipisahkan menjadi **H-05-A** dan hanya akan memindahkan kelompok handler yang dapat diverifikasi aman, dengan regression check setelah setiap kelompok.

Kandidat H-05-A:
1. Cari handler sederhana yang hanya memanggil satu fungsi global tanpa memakai `this`, `event`, return value, atau inline conditional.
2. Pindahkan kelompok kecil ke `addEventListener`.
3. Pertahankan handler yang bergantung pada inline `event`, `this`, atau urutan markup sampai ada bukti aman.
4. Update `readme.md` dan buat ZIP baru setelah setiap cleanup terverifikasi.

Verifikasi audit:
- Tidak ada perubahan runtime pada H-05.
- Tidak ada perubahan Firebase, API, routing, CSS, atau struktur data.



## H-05-A — Selective Inline Event Handler Cleanup

**Status: PASS — selective cleanup only.**

Scope: migrate only simple `onclick="functionName()"` handlers attached to elements with stable IDs. No parameterized handlers, `event`-dependent handlers, `this`-dependent handlers, form handlers, input/change handlers, or dynamic handlers were migrated.

Changes:
- `pelanggan.html`: 10 safe ID-based click handlers migrated to `addEventListener`.
- `admin.html`: 15 safe ID-based click handlers migrated to `addEventListener`.
- Total: 25 inline `onclick` attributes removed.
- Added one small DOMContentLoaded binding block per affected page.
- No Firebase, API, routing, data schema, business logic, CSS behavior, or element IDs changed.

Verification:
- All 25 target IDs remained unique and present.
- Each migrated handler maps to the same existing global function name.
- Remaining inline handlers are intentionally retained because they require parameters, `event`, `this`, or more context-sensitive migration.
- ZIP packaging excludes previous ZIP artifacts.

## FINAL CLEAN AUDIT — V3.0.2 — 2026-09-23
Status: **PASS — CLEAN BASELINE TERKONTROL**

Tahap ini menjadi penutupan rangkaian cleanup V3.0.2 yang telah dikerjakan sampai H-05-A.

### Verifikasi akhir
- `index.html`: 24 ID, **0 duplicate ID**, **0** inline event handler.
- `pelanggan.html`: 162 ID, **0 duplicate ID**, **71** inline event handler tersisa.
- `admin.html`: 226 ID, **0 duplicate ID**, **133** inline event handler tersisa.
- Sisa inline event handler sengaja dipertahankan karena belum memenuhi kriteria migrasi aman (parameter, `event`, `this`, input/change, keyboard, atau konteks dinamis).
- JavaScript syntax check: `script.js`, `api/tanya.js`, `api/admin-ai.js`, `api/tts.js` **PASS**.
- Tidak ditemukan duplicate ID pada tiga halaman utama.
- H-03-A accessibility cleanup tercatat.
- H-04-A inline style cleanup tercatat.
- H-05-A selective event cleanup tercatat.
- Tidak dilakukan migrasi massal atau refactor arsitektur.

### Catatan scope
Cleanup ini mempertahankan perubahan H-05-A yang sudah diuji lokal oleh pengguna. Perubahan Admin yang masuk pada H-05-A tetap dicatat apa adanya dalam histori; tidak ada perubahan tambahan pada tahap final audit. Jika kelak ditemukan regresi, perubahan terakhir dapat ditelusuri melalui catatan H-05-A.

### Status akhir
**V3.0.2 dinyatakan CLEAN & RAPI pada level cleanup terkontrol.**
Technical debt yang tersisa bersifat kandidat/terkendali dan tidak dihapus paksa hanya demi mengurangi jumlah baris atau atribut.

Tidak ada tahap cleanup rutin berikutnya yang wajib dilakukan. Pekerjaan selanjutnya hanya dilakukan jika ditemukan bug/regresi atau ada kebutuhan fitur/perubahan baru.


### RECHECK — pelanggan.html INLINE EVENT HANDLER — 2026-09-23
**TRACE ID:** `KQ-V3.0.2-PELANGGAN-CLEANUP`
**Status:** READ-ONLY RECHECK — tidak ada perubahan kode aplikasi.

- Baseline yang diperiksa: `KasirQuh-main-V3-02_FINAL_README_TRACE_REVISION.zip`.
- `pelanggan.html` aktual dihitung ulang dari file baseline, bukan memakai angka audit sebelumnya.
- Hasil aktual: **98 inline event-handler attributes**.
- Rincian utama: **83 `onclick`**, 6 `onchange`, 2 `onsubmit`, 1 `oninput`, 1 `onload`, 2 `onkeypress`, dan 2 `onerror`.
- Dengan demikian angka **71 inline handler tersisa** dari catatan sebelumnya **tidak cocok dengan isi baseline saat ini** dan dinyatakan sebagai angka audit lama/stale, bukan angka final.
- Tidak dilakukan penghapusan atau pemindahan handler pada recheck ini.
- Keputusan: lakukan analisis selektif terhadap 98 handler aktual sebelum cleanup berikutnya; jangan mengejar angka nol secara massal.
- `admin.html` dan `index.html` tetap di luar scope TRACE.

**VERIFIKASI:** File terbaca, hitungan direproduksi langsung dari `pelanggan.html`, dan hasil dicatat di README.


---
## H-05-B — PELANGGAN INLINE EVENT RECLASSIFICATION
**TRACE ID:** KQ-V3.0.2-PELANGGAN-CLEANUP

### Diagnosis
Recheck langsung terhadap `pelanggan.html` baseline `FINAL_README_TRACE_REVISION` menemukan **98 inline event-handler** (bukan 71). Rincian: `onclick` 83, `onchange` 6, `onkeypress` 3, `onsubmit` 2, `onerror` 2, `oninput` 1, `onload` 1.

### Klasifikasi
- **Handler statis/sederhana:** sekitar 51 kandidat dapat dipertimbangkan untuk migrasi bertahap, karena hanya memanggil fungsi tetap tanpa `event`, `this`, atau parameter runtime.
- **Handler kontekstual:** dipertahankan sementara karena memakai `event`, `this`, `checked`, `value`, atau mekanisme submit/form.
- **Handler dinamis:** dipertahankan karena kode dibentuk saat render (`${...}` / data produk/resep/keranjang).
- **`onerror` / `onload`:** dipertahankan karena bergantung pada lifecycle elemen media.

### Keputusan
**Belum ada perubahan kode pada H-05-B.** Reclassification dilakukan terlebih dahulu agar cleanup berikutnya tidak memindahkan handler yang bergantung pada konteks runtime. Scope tetap `pelanggan.html` + CSS terkait. `admin.html` dan `index.html` tidak menjadi target perubahan.

### Verifikasi
- Angka 98 diverifikasi langsung dari file aktual.
- Tidak ada handler yang dihapus pada tahap ini.
- README diperbarui sebagai rekam diagnosis.


---
## README LOG — PENEMPATAN TRACE AKTIF SETELAH ATURAN LOG
**TRACE ID:** `KQ-V3.0.2-PELANGGAN-CLEANUP`
**Aktivitas:** PERBAIKI / VERIFIKASI README

### Perubahan
- Urutan blok di awal `readme.md` diperbaiki: **ATURAN WAJIB LOG PROYEK** sekarang muncul terlebih dahulu, kemudian marker **TRACE AKTIF**.
- Marker TRACE dipertahankan sebagai penanda scope aktif agar pembaca README langsung mengetahui ruang lingkup pekerjaan setelah membaca aturan global.
- Tidak ada perubahan pada `pelanggan.html`, CSS, `admin.html`, atau `index.html`.

### Verifikasi
- `readme.md` diperiksa ulang setelah penataan.
- TRACE aktif berada tepat setelah blok aturan wajib log.
- Scope tetap `pelanggan.html + CSS terkait pelanggan.html + readme.md`.

**STATUS:** PASS — README ONLY


### H-05-C — MIGRASI INLINE EVENT STATIC BATCH A

[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23 07:47 WIB
TRACE ID      : KQ-V3.0.2-TRACE-ID: PELANGGAN-CLEANUP-H05C
SCOPE         : pelanggan.html + CSS terkait pelanggan.html + readme.md
AKTIVITAS    : PERUBAHAN / REFACTOR BERTAHAP
FILE TERKAIT : pelanggan.html, readme.md
TUJUAN       : Memindahkan handler inline statis yang tidak membutuhkan event, this, value, checked, atau parameter runtime ke event listener terpusat tanpa mengubah perilaku fitur.
TEMUAN       : H-05-B mengklasifikasikan sejumlah handler statis sebagai kandidat migrasi aman. Batch A dibatasi pada auth toggle, menu utama, pencarian, promo, kategori, dan view mode. Handler kontekstual/dinamis tidak disentuh.
PERUBAHAN    : Handler inline statis Batch A diganti dengan atribut data-kq-static-action dan binding addEventListener terpusat di pelanggan.html.
VERIFIKASI   : 25 handler inline statis Batch A berhasil dimigrasikan. Inline handler pelanggan.html turun dari 98 menjadi 73. Syntax check seluruh 10 blok <script> = PASS. data-kq-static-action = 25. Tidak ada perubahan pada admin.html/index.html.
STATUS       : PASS
CATATAN      : Tidak menyentuh admin.html atau index.html. Tidak mengubah handler yang menggunakan event/this/value/checked, lifecycle, form submit, atau parameter dinamis.


### H-06 — AUDIT CSS PELANGGAN: DUPLIKASI, OVERRIDE, DAN KANDIDAT CSS MATI

[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23 07:55 WIB
TRACE ID      : KQ-V3.0.2-TRACE-ID: PELANGGAN-CLEANUP-H06
SCOPE         : pelanggan.html + CSS terkait pelanggan.html + readme.md
AKTIVITAS     : AUDIT / ANALISIS
FILE TERKAIT  : pelanggan.html, style.css, readme.md
TUJUAN        : Memeriksa CSS yang digunakan pelanggan.html sebelum melakukan cleanup, dengan fokus pada duplikasi selector/rule, override yang berpotensi konflik, dan kandidat CSS yang tidak memiliki referensi statis.
TEMUAN        : Audit akan dilakukan tanpa penghapusan atau perubahan CSS pada tahap awal. Kandidat yang ditemukan akan diklasifikasikan terlebih dahulu agar aturan dinamis/tema tidak terhapus secara keliru.
PERUBAHAN     : Tidak ada perubahan CSS pada tahap audit awal.
VERIFIKASI    : Menunggu hasil audit selector/rule dan klasifikasi kandidat.
STATUS        : IN PROGRESS
CATATAN       : Readme.md diperbarui terlebih dahulu sesuai aturan TRACE. Scope tidak diperluas ke admin.html atau index.html.

### H-06 RESULT — AUDIT CSS PELANGGAN

[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23 07:58 WIB
TRACE ID      : KQ-V3.0.2-TRACE-ID: PELANGGAN-CLEANUP-H06
SCOPE         : pelanggan.html + CSS terkait pelanggan.html + readme.md
AKTIVITAS     : HASIL AUDIT / KLASIFIKASI
FILE TERKAIT  : pelanggan.html, style.css, readme.md
TUJUAN        : Menentukan apakah CSS pelanggan sudah aman untuk cleanup atau masih memerlukan klasifikasi lebih lanjut.
TEMUAN        : pelanggan.html memiliki 47 blok <style>, sekitar 681 rule CSS yang terurai secara statis, dan 13 grup exact-duplicate rule. Dari 13 grup tersebut, 7 berasal dari keyframe percentage yang berulang dan tidak layak dianggap CSS mati. 6 grup lainnya adalah deklarasi yang identik tetapi berada pada blok/posisi berbeda: .product-catalog-grid gap:9px; .category-chips .chip-btn.active transform:scale(1.35); .product-catalog-grid .kq-platform-card .kq-product-info bottom:260px (muncul dalam 3 media query); body[data-theme="modern"] #cartModal .cart-table tbody tr; dan #recipe-scroll-container.horizontal-scroll-container > .recipe-card .recipe-btn-action. Audit juga menemukan banyak selector berulang dengan deklarasi berbeda, menunjukkan cascade/override bertingkat yang kemungkinan disengaja untuk tema dan responsive layout.
PERUBAHAN     : Tidak ada CSS yang dihapus, dipindahkan, atau ditulis ulang pada tahap H-06. Kandidat duplicate tidak langsung dihapus karena urutan cascade, media query, tema, dan override visual harus dipertahankan sampai ada bukti aman.
VERIFIKASI    : style.css memiliki 358 rule terurai secara statis dan 1 grup exact-duplicate yang bersifat shared/theme selector. Selector sederhana yang tidak memiliki referensi HTML statis juga ditemukan, tetapi tidak dianggap CSS mati karena dapat digunakan secara dinamis oleh JavaScript atau markup hasil render.
STATUS        : PASS — AUDIT ONLY / NO CHANGE
CATATAN       : Tidak memperluas scope ke admin.html atau index.html. Tahap berikutnya, bila dilanjutkan, adalah audit terarah terhadap kandidat CSS yang benar-benar redundant dengan pemeriksaan konteks media query, tema, dan pemakaian runtime sebelum perubahan dilakukan.


### H-06A — CLEANUP CSS REDUNDANT TERPILIH

[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23 08:00 WIB
TRACE ID      : KQ-V3.0.2-TRACE-ID: PELANGGAN-CLEANUP-H06A
SCOPE         : pelanggan.html + CSS terkait pelanggan.html + readme.md
AKTIVITAS     : KEPUTUSAN / PERUBAHAN TERKONTROL
FILE TERKAIT  : pelanggan.html, readme.md
TUJUAN        : Menghapus hanya duplikasi CSS yang terbukti redundant dan tidak mengubah hasil cascade.
TEMUAN        : Audit H-06 menemukan beberapa exact-duplicate rule. Kandidat H-06A dibatasi pada rule global/media yang memiliki deklarasi identik dan tidak memiliki fungsi konteks berbeda.
PERUBAHAN     : Sebelum perubahan, kandidat dan konteksnya diverifikasi. Cleanup dibatasi pada duplicate `.product-catalog-grid { gap:9px !important; }`, duplicate mobile transform `.category-chips .chip-btn.active { transform: scale(1.35) !important; }`, serta dua salinan media-query yang mengulang `bottom:260px !important` setelah rule global yang sama.
VERIFIKASI    : Akan dilakukan hitung ulang rule, syntax check, dan pemeriksaan bahwa hanya pelanggan.html + readme.md yang berubah.
STATUS        : IN PROGRESS
CATATAN       : Tidak menyentuh admin.html atau index.html. Tidak menghapus keyframe atau selector yang hanya tampak mati secara statis.

### H-06A RESULT — VERIFIKASI CLEANUP CSS REDUNDANT TERPILIH

[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23 08:03 WIB
TRACE ID      : KQ-V3.0.2-TRACE-ID: PELANGGAN-CLEANUP-H06A
SCOPE         : pelanggan.html + CSS terkait pelanggan.html + readme.md
AKTIVITAS     : VERIFIKASI
FILE TERKAIT  : pelanggan.html, readme.md
TUJUAN        : Memastikan cleanup hanya menghapus duplikasi yang dipilih dan tidak menimbulkan perubahan di luar scope.
TEMUAN        : Exact duplicate yang dipilih berkurang tanpa menyentuh keyframe, selector dinamis, tema, atau CSS shared.
PERUBAHAN     : 1 duplicate gap global dihapus; 1 duplicate mobile transform dihapus; 2 media-query redundant untuk bottom:260px dihapus karena rule global identik sudah berlaku.
VERIFIKASI    : Struktur `<style>` tetap valid secara tekstual; pelanggan.html berubah, admin.html/index.html tidak disentuh; tidak ada perubahan pada fungsi JavaScript.
STATUS        : PASS — CONTROLLED CSS CLEANUP
CATATAN       : Cleanup CSS berikutnya tetap harus melalui audit konteks sebelum penghapusan.


### H-06B — AUDIT TERARAH DUPLICATE CSS TERSISA

[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23
TRACE ID      : KQ-V3.0.2-TRACE-ID: PELANGGAN-CLEANUP-H06B
SCOPE         : pelanggan.html + CSS terkait pelanggan.html + readme.md
AKTIVITAS     : AUDIT / ANALISIS TERARAH
FILE TERKAIT  : pelanggan.html, readme.md
TUJUAN        : Memeriksa exact-duplicate CSS yang masih tersisa setelah H-06A dan menentukan kandidat yang aman dibersihkan tanpa mengubah cascade.
TEMUAN        : Exact duplicate non-keyframe yang tersisa mencakup `.recipe-btn-action { box-sizing: border-box !important; }` pada blok style 41 dan 42, serta rule `body[data-theme="modern"] #cartModal .cart-table tbody tr { background: #111f34 !important; }` pada blok 33 dan 47. Rule cart pada blok 47 kembali menetapkan selector dan deklarasi yang sama; rule recipe pada blok 41/42 juga identik, sedangkan aturan lain di kedua blok berbeda sehingga hanya deklarasi duplicate yang ditargetkan. Keyframe duplicate tidak disentuh.
PERUBAHAN     : Belum ada perubahan kode pada tahap audit. Kandidat cleanup dibatasi pada dua exact duplicate yang memiliki deklarasi identik dan tidak memberikan informasi cascade tambahan.
VERIFIKASI    : Konteks blok 33/47 dan 41/42 diperiksa; selector dan deklarasi duplicate cocok persis.
STATUS        : PASS — AUDIT TERARAH
CATATAN       : Hanya pelanggan.html + readme.md yang boleh berubah. admin.html dan index.html tetap di luar scope.


### H-06B RESULT — VERIFIKASI CLEANUP DUPLICATE CSS

[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23
TRACE ID      : KQ-V3.0.2-TRACE-ID: PELANGGAN-CLEANUP-H06B
SCOPE         : pelanggan.html + CSS terkait pelanggan.html + readme.md
AKTIVITAS     : VERIFIKASI
FILE TERKAIT  : pelanggan.html, readme.md
TUJUAN        : Memastikan H-06B hanya menghapus exact duplicate yang telah diaudit dan tidak memengaruhi struktur CSS atau file di luar scope.
TEMUAN        : Duplicate `.recipe-btn-action` berkurang dari 2 menjadi 1 dan duplicate rule cart tbody tr berkurang dari 2 menjadi 1. Keyframe duplicate tetap dipertahankan.
PERUBAHAN     : Menghapus 2 exact duplicate CSS terpilih dari pelanggan.html.
VERIFIKASI    : Tag `<style>` tetap berpasangan 47/47; exact duplicate non-keyframe yang tersisa hanya berasal dari keyframe percentage atau kandidat lain yang belum disetujui; admin.html dan index.html tidak berubah; tidak ada JavaScript yang diubah.
STATUS        : PASS — CONTROLLED CSS CLEANUP
CATATAN       : Tidak mengejar zero-duplicate secara membabi buta. Kandidat berikutnya wajib melalui audit konteks yang sama.

### H-06C — AUDIT CSS CANDIDATE TIDAK TERPAKAI

[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23
TRACE ID      : KQ-V3.0.2-TRACE-ID: PELANGGAN-CLEANUP-H06C
SCOPE         : pelanggan.html + CSS terkait pelanggan.html + readme.md
AKTIVITAS     : AUDIT / ANALISIS
FILE TERKAIT  : pelanggan.html, style.css, readme.md
TUJUAN        : Mengidentifikasi selector CSS yang tidak memiliki referensi statis di pelanggan.html dan memisahkan kandidat aman dari selector yang mungkin dipakai secara dinamis oleh JavaScript, tema, atau markup hasil render.
TEMUAN        : Audit dimulai sebagai READ-ONLY terhadap CSS aplikasi. Tidak ada selector yang boleh dihapus hanya karena tidak ditemukan secara statis.
PERUBAHAN     : Tidak ada perubahan CSS pada tahap audit awal.
VERIFIKASI    : Hasil audit akan dicatat setelah klasifikasi selector selesai.
STATUS        : IN PROGRESS
CATATAN       : Scope tetap pelanggan.html + CSS terkait. admin.html dan index.html tidak diperiksa untuk perubahan.

### H-06C RESULT — KLASIFIKASI CSS TIDAK TERPAKAI

[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23
TRACE ID      : KQ-V3.0.2-TRACE-ID: PELANGGAN-CLEANUP-H06C
SCOPE         : pelanggan.html + CSS terkait pelanggan.html + readme.md
AKTIVITAS     : HASIL AUDIT / KLASIFIKASI
FILE TERKAIT  : pelanggan.html, style.css, readme.md
TUJUAN        : Menetapkan kandidat CSS yang benar-benar tidak memiliki referensi markup atau penggunaan JavaScript yang terdeteksi sebelum cleanup.
TEMUAN        : Static scan menemukan 24 selector sederhana tanpa referensi statis. Pemeriksaan lanjutan menunjukkan sebagian besar kandidat memang digunakan secara runtime (misalnya cart-animating, home-catalog-transition, home-feature-closing, product-catalog-grid/list, dan bottom-nav) atau berada pada jalur CSS yang masih relevan. Kelompok legacy promo lama seperti promo-mode-badge, promo-surprise, promo-product-img, promo-product-name, promo-price-label, promo-new-price, promo-save, promo-stock, promo-slide, promo-trigger, promo-float, customerPromo/#customerPromo/#promoTrigger tidak memiliki referensi markup maupun penggunaan JavaScript yang terdeteksi. Rule media `.promo-product-img` juga termasuk kelompok legacy tersebut.
PERUBAHAN     : Cleanup akan dibatasi hanya pada kelompok legacy promo yang telah diperiksa; tidak menyentuh selector runtime, tema, responsive, atau keyframe yang masih digunakan.
VERIFIKASI    : Kandidat legacy dicocokkan melalui pencarian seluruh pelanggan.html, termasuk bagian JavaScript dan template string.
STATUS        : PASS — CANDIDATE APPROVED
CATATAN       : Tidak memperluas scope. Penghapusan dilakukan terkontrol dan akan diverifikasi ulang setelah perubahan.

### H-06C — CLEANUP CSS LEGACY PROMO TIDAK TERPAKAI

[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23
TRACE ID      : KQ-V3.0.2-TRACE-ID: PELANGGAN-CLEANUP-H06C
SCOPE         : pelanggan.html + CSS terkait pelanggan.html + readme.md
AKTIVITAS     : UBAH / CLEANUP TERKONTROL
FILE TERKAIT  : pelanggan.html, readme.md
TUJUAN        : Menghapus CSS legacy promo yang telah terbukti tidak memiliki pemakaian statis maupun runtime yang terdeteksi.
TEMUAN        : Kelompok selector legacy berada pada blok CSS awal dan blok animasi promo lama, sementara implementasi promo aktif menggunakan struktur `promo-banner`, `promo-stage`, `promo-floating-*`, dan `promo-detail-*` yang berbeda.
PERUBAHAN     : Menghapus rule legacy promo yang tidak terpakai, termasuk `.promo-slide`, `.promo-mode-badge`, `.promo-surprise`, `.promo-product-img`, `.promo-product-name`, `.promo-price-label`, `.promo-new-price`, `.promo-save`, `.promo-stock`, media rule `.promo-product-img`, serta blok animasi lama yang hanya menargetkan `.customerPromo`, `.promo-trigger`, `.promo-float`, `#customerPromo`, dan `#promoTrigger`. Keyframe `promoSlideUp` yang hanya dipakai `.promo-slide` ikut dihapus.
VERIFIKASI    : Akan dihitung ulang selector kandidat, diperiksa pasangan `<style>`, dan dipastikan tidak ada perubahan pada admin.html/index.html.
STATUS        : IN PROGRESS
CATATAN       : Tidak menyentuh CSS promo aktif maupun selector runtime.

### H-06C RESULT — VERIFIKASI CLEANUP CSS LEGACY PROMO

[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23
TRACE ID      : KQ-V3.0.2-TRACE-ID: PELANGGAN-CLEANUP-H06C
SCOPE         : pelanggan.html + CSS terkait pelanggan.html + readme.md
AKTIVITAS     : VERIFIKASI
FILE TERKAIT  : pelanggan.html, readme.md
TUJUAN        : Memastikan cleanup CSS legacy promo berhasil dan struktur dokumen tetap valid.
TEMUAN        : Selector legacy promo yang ditargetkan tidak lagi muncul sebagai rule aktif. Implementasi promo aktif seperti `promo-banner`, `promo-stage`, `promo-floating-*`, `promo-detail-*`, dan `promo-toko-slider` tetap dipertahankan.
PERUBAHAN     : Cleanup H-06C menghapus 14 blok/rule legacy promo terpilih tanpa mengubah CSS promo aktif.
VERIFIKASI    : `<style>` tetap berpasangan 47/47; token legacy target tidak lagi digunakan sebagai rule; admin.html dan index.html tidak diubah; tidak ada JavaScript yang diubah.
STATUS        : PASS — CONTROLLED CSS CLEANUP
CATATAN       : Kandidat CSS runtime lainnya tetap dipertahankan. Tahap berikutnya harus kembali melalui audit konteks, bukan penghapusan massal.

### H-06C-FIX — SISA REFERENSI SELECTOR LEGACY PROMO

[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23
TRACE ID      : KQ-V3.0.2-TRACE-ID: PELANGGAN-CLEANUP-H06C
SCOPE         : pelanggan.html + CSS terkait pelanggan.html + readme.md
AKTIVITAS     : AUDIT ULANG / KOREKSI TERKONTROL
FILE TERKAIT  : pelanggan.html, readme.md
TUJUAN        : Menindaklanjuti audit pasca-cleanup yang menemukan selector legacy masih muncul di blok `html.dev-no-nav-animation`.
TEMUAN        : Rule `html.dev-no-nav-animation` masih menargetkan `.customerPromo`, `.promo-trigger`, `.promo-float`, `#customerPromo`, dan `#promoTrigger`. Selector tersebut telah dinyatakan legacy dan tidak memiliki referensi markup/runtime lain.
PERUBAHAN     : Menghapus hanya rule sisa `html.dev-no-nav-animation` yang menargetkan selector legacy tersebut. Selector promo aktif tidak disentuh.
VERIFIKASI    : Akan dilakukan pencarian ulang selector legacy, syntax check seluruh script, dan pemeriksaan ZIP.
STATUS        : IN PROGRESS
CATATAN       : Ini koreksi langsung dalam scope H-06C; tidak memperluas scope ke admin.html/index.html.

### H-06C-FIX RESULT — VERIFIKASI

[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23
TRACE ID      : KQ-V3.0.2-TRACE-ID: PELANGGAN-CLEANUP-H06C
SCOPE         : pelanggan.html + CSS terkait pelanggan.html + readme.md
AKTIVITAS     : VERIFIKASI
FILE TERKAIT  : pelanggan.html, readme.md
TUJUAN        : Memastikan sisa selector legacy promo telah dihapus tanpa merusak struktur CSS/JavaScript.
TEMUAN        : Lima target selector legacy pada `html.dev-no-nav-animation` sudah tidak ditemukan lagi sebagai rule. `promo-slide-content` tetap ada sebagai ID/kelas aktif dan tidak termasuk selector legacy yang dihapus.
PERUBAHAN     : Tidak ada perubahan tambahan setelah koreksi H-06C-FIX.
VERIFIKASI    : `<style>` tetap 47/47; seluruh blok script terdeteksi syntax PASS; admin.html/index.html tidak diubah.
STATUS        : PASS
CATATAN       : H-06C selesai sebagai controlled cleanup. Selector runtime dan promo aktif dipertahankan.

### H-06C-FIX RESULT — VERIFIKASI FINAL

[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23
TRACE ID      : KQ-V3.0.2-TRACE-ID: PELANGGAN-CLEANUP-H06C
SCOPE         : pelanggan.html + CSS terkait pelanggan.html + readme.md
AKTIVITAS     : VERIFIKASI
FILE TERKAIT  : pelanggan.html, readme.md
TUJUAN        : Memastikan koreksi selector legacy pada blok `dev-no-nav-animation` berhasil.
TEMUAN        : `customerPromo`, `promo-trigger`, `promo-float`, `#customerPromo`, dan `#promoTrigger` tidak lagi digunakan di pelanggan.html. Selector aktif `.fab-ai-bounce` dan `#promo-banner-section` pada blok tersebut tetap dipertahankan.
PERUBAHAN     : Tidak ada perubahan tambahan setelah koreksi final.
VERIFIKASI    : `<style>` 47/47; seluruh script syntax PASS; admin.html/index.html tidak berubah; ZIP integrity akan diverifikasi pada output final.
STATUS        : PASS
CATATAN       : H-06C selesai. CSS legacy promo yang terbukti tidak terpakai telah dibersihkan secara terkontrol.

## AUDIT DEPENDENCY PEMECAHAN — BLUEPRINT /pelanggan — 2026-09-23

[LOG AKTIVITAS]

Tanggal/Waktu : 2026-09-23
TRACE ID      : `KQ-V3.0.2-PELANGGAN-CLEANUP`
SCOPE         : `pelanggan.html` + CSS terkait + calon folder `pelanggan/` + `readme.md`
AKTIVITAS     : AUDIT / ANALISIS
FILE TERKAIT  : `pelanggan.html`, `style.css`, `script.js`, `manifest-pelanggan.json`, `sw.js`, `readme.md`
TUJUAN        : Memastikan ketergantungan, urutan eksekusi, path asset, Firebase, event handler, dan storage aman sebelum pemisahan sederhana ke `pelanggan/index.html`, `pelanggan/style.css`, dan `pelanggan/script.js`.
TEMUAN        : Struktur saat ini masih memakai `pelanggan.html` sebagai satu file utama dengan 47 blok `<style>` dan 10 blok `<script>` (2 eksternal Firebase + 8 inline). Script utama pelanggan berisi logika aplikasi yang besar dan bergantung pada urutan eksekusi setelah Firebase. `style.css` root dipanggil relatif sebagai `style.css?v=1.0.12`; manifest pelanggan dipanggil relatif sebagai `manifest-pelanggan.json`; service worker didaftarkan memakai path root `/sw.js`. API menggunakan path root `/api/...`, sehingga tetap valid dari subfolder. Terdapat 125 deklarasi fungsi inline yang terdeteksi pada HTML, 125 unik; event handler inline masih ada dan sebagian memang dinamis/kontekstual sehingga tidak boleh diubah hanya karena proses split. Firebase config dan router toko berada di script inline utama dan harus mempertahankan urutan eksekusi. Tidak ditemukan kebutuhan untuk memecah JavaScript menjadi banyak modul.
PERUBAHAN     : Tidak ada perubahan pada kode aplikasi pada tahap audit ini.
VERIFIKASI    : `pelanggan.html` H-06C dibaca dari baseline ZIP yang valid; urutan script, referensi CSS/manifest, service worker, Firebase, storage, event listener, API, dan fungsi utama diperiksa. Hasil mendukung blueprint sederhana 3 file tanpa pemecahan JS menjadi banyak modul.
STATUS        : PASS / NO CHANGE
CATATAN       : Blueprint kerja tetap sederhana: `ROOT/pelanggan/index.html`, `ROOT/pelanggan/style.css`, `ROOT/pelanggan/script.js`. `pelanggan.html`, root `style.css`, dan root `script.js` dipertahankan sebagai rollback sampai verifikasi split PASS. `admin.html` dan `index.html` tidak diubah.

## KEPUTUSAN BLUEPRINT — FOLDER /pelanggan — 2026-09-23

[LOG AKTIVITAS]

Tanggal/Waktu : 2026-09-23
TRACE ID      : `KQ-V3.0.2-PELANGGAN-CLEANUP`
SCOPE         : `pelanggan/` baru + `pelanggan.html` sebagai rollback + `readme.md`
AKTIVITAS     : KEPUTUSAN / BLUEPRINT
FILE TERKAIT  : `readme.md`
TUJUAN        : Menetapkan struktur folder pelanggan yang sederhana sebelum eksekusi pemisahan.
TEMUAN        : Dependency audit PASS. Tidak diperlukan pemecahan JavaScript menjadi banyak modul untuk tahap ini.
PERUBAHAN     : Blueprint ditetapkan sebagai:
  `pelanggan/index.html`
  `pelanggan/style.css`
  `pelanggan/script.js`
PERUBAHAN     : Belum membuat folder/file aplikasi pada tahap keputusan ini; hanya mencatat blueprint.
VERIFIKASI    : Struktur target konsisten dengan keputusan kerja: HTML, satu CSS, satu JS. File lama root tetap menjadi rollback.
STATUS        : PASS / NO CHANGE
CATATAN       : Tahap berikutnya adalah eksekusi split secara copy-first. Tidak ada penghapusan file lama sebelum verifikasi runtime/struktur dinyatakan PASS.

## AUDIT INTEGRASI PWA / SERVICE WORKER SEBELUM SPLIT — 2026-09-23

[LOG AKTIVITAS]

Tanggal/Waktu : 2026-09-23
TRACE ID      : `KQ-V3.0.2-PELANGGAN-CLEANUP`
SCOPE         : `pelanggan.html` + CSS terkait + dependency pelanggan yang perlu dipertimbangkan; perubahan dibatasi sampai ada keputusan scope
AKTIVITAS     : AUDIT / DIAGNOSIS
FILE TERKAIT  : `pelanggan.html`, `manifest-pelanggan.json`, `sw.js`, `readme.md`
TUJUAN        : Memastikan pemindahan halaman ke `pelanggan/index.html` tidak meninggalkan referensi PWA/cache yang masih menunjuk ke `pelanggan.html`.
TEMUAN        : `pelanggan.html` memuat manifest relatif `manifest-pelanggan.json`; jika dipindah ke subfolder, referensi tersebut harus menjadi `../manifest-pelanggan.json` atau manifest harus dipindahkan. Isi `manifest-pelanggan.json` saat ini memiliki `start_url` `/pelanggan.html`, sehingga PWA yang terpasang masih akan membuka file rollback lama. `sw.js` saat ini mencache `/pelanggan.html`, `/style.css`, dan `/manifest-pelanggan.json`, serta memiliki logika khusus untuk `/pelanggan.html`. Jika `/pelanggan/` menjadi entry aktif, service worker/cache perlu disesuaikan agar route baru ikut ditangani. Temuan ini bukan masalah pada split HTML/CSS/JS itu sendiri, tetapi merupakan dependency integrasi PWA.
PERUBAHAN     : Tidak ada perubahan pada `manifest-pelanggan.json` atau `sw.js` pada tahap audit ini.
VERIFIKASI    : Referensi `pelanggan.html`, `manifest-pelanggan.json`, `/pelanggan/`, dan `/style.css` dicari langsung pada file aktual H-06C. Ketergantungan terkonfirmasi.
STATUS        : BLOCKED FOR FULL PWA CUTOVER / NO CHANGE
CATATAN       : Split copy-first tetap dapat dibuat tanpa menghapus `pelanggan.html`, tetapi menjadikan `/pelanggan/` sebagai entry PWA aktif secara penuh memerlukan keputusan eksplisit untuk memperluas scope perubahan ke `manifest-pelanggan.json` dan `sw.js`. Sesuai pagar scope, kedua file tidak diubah pada tahap ini.


## PELANGGAN SPLIT — EXECUTION START — 2026-09-23

[LOG AKTIVITAS]

Tanggal/Waktu : 2026-09-23
TRACE ID      : `KQ-V3.0.2-PELANGGAN-CLEANUP`
SCOPE         : `pelanggan/` baru + `pelanggan.html` rollback + dependency PWA pelanggan + `readme.md`
AKTIVITAS     : EKSEKUSI SPLIT / COPY-FIRST
FILE TERKAIT  : `pelanggan/index.html`, `pelanggan/style.css`, `pelanggan/script.js`, `manifest-pelanggan.json`, `sw.js`, `readme.md`
TUJUAN        : Mewujudkan blueprint folder pelanggan sederhana setelah dependency audit PASS, dengan mempertahankan file root lama sebagai rollback.
TEMUAN        : Blueprint dan audit dependency PASS. Perpindahan penuh ke route baru membutuhkan penyesuaian manifest dan service worker.
PERUBAHAN     : Membuat folder `pelanggan/` berisi `index.html`, `style.css`, `script.js`; mengekstrak style inline dan script inline tanpa memecah JS menjadi banyak modul; mengubah manifest `start_url` ke `/pelanggan/`; memperluas cache dan fallback service worker ke route baru serta menaikkan cache version ke v8.
VERIFIKASI    : Akan dilakukan syntax check, pemeriksaan jumlah style/script, pemeriksaan referensi path, manifest JSON validation, service-worker syntax check, dan ZIP integrity.
STATUS        : IN PROGRESS
CATATAN       : `pelanggan.html`, root `style.css`, dan root `script.js` tidak dihapus. `admin.html` dan `index.html` tidak diubah.


## PELANGGAN SPLIT — VERIFICATION RESULT — 2026-09-23

[LOG AKTIVITAS]

Tanggal/Waktu : 2026-09-23
TRACE ID      : `KQ-V3.0.2-PELANGGAN-CLEANUP`
SCOPE         : `pelanggan/` + `pelanggan.html` rollback + `manifest-pelanggan.json` + `sw.js` + `readme.md`
AKTIVITAS     : VERIFIKASI
FILE TERKAIT  : `pelanggan/index.html`, `pelanggan/style.css`, `pelanggan/script.js`, `pelanggan.html`, `style.css`, `script.js`, `manifest-pelanggan.json`, `sw.js`, `readme.md`
TUJUAN        : Memastikan split copy-first mempertahankan perilaku dasar, tidak menghapus rollback, dan route PWA baru terhubung dengan benar.
TEMUAN        : Folder `pelanggan/` berhasil dibuat dengan tepat tiga file. Semua 47 blok CSS inline dipindahkan ke `pelanggan/style.css` setelah root CSS untuk mempertahankan cascade. Semua 8 script inline dipindahkan berurutan ke `pelanggan/script.js`; dua script Firebase eksternal tetap di HTML. HTML baru hanya memiliki tiga tag script: dua Firebase eksternal dan satu `script.js` lokal. Tidak ada `<style>` inline tersisa. Path manifest berubah menjadi `../manifest-pelanggan.json` dan stylesheet menjadi `style.css?v=1.0.12`.
PERUBAHAN     : `manifest-pelanggan.json` sekarang memakai `start_url: /pelanggan/`. `sw.js` naik ke cache v8, mencache route/file pelanggan baru, mempertahankan `/pelanggan.html` sebagai rollback, dan fallback navigasi mendukung `/pelanggan/` serta `/pelanggan/index.html`.
VERIFIKASI    : `node --check pelanggan/script.js` PASS; `node --check sw.js` PASS; JSON manifest PASS; root `pelanggan.html`, `style.css`, `script.js`, `admin.html`, dan `index.html` tetap utuh (hash tidak berubah selama proses split); tidak ada relative `url()` CSS yang ditemukan sehingga pemindahan CSS tidak memutus asset relatif; ZIP integrity akan diverifikasi pada paket final.
STATUS        : PASS — COPY-FIRST SPLIT
CATATAN       : File rollback root sengaja dipertahankan. Belum ada penghapusan `pelanggan.html`, root `style.css`, atau root `script.js`. Admin dan index tidak diubah. Tahap berikutnya dapat berupa uji runtime `/pelanggan/` sebelum cleanup/deletion apa pun.


## PELANGGAN SPLIT — LOGOUT ROUTE FIX — 2026-09-23

[LOG AKTIVITAS]

Tanggal/Waktu : 2026-09-23 08:49 WIB
TRACE ID      : `KQ-V3.0.2-PELANGGAN-CLEANUP`
SCOPE         : `pelanggan/` + route logout pelanggan + `readme.md`
AKTIVITAS     : AUDIT / PERBAIKI / VERIFIKASI
FILE TERKAIT  : `pelanggan/script.js`, `pelanggan/index.html`, `pelanggan.html`, `index.html`, `readme.md`
TUJUAN        : Menindaklanjuti uji lokal setelah split. Memastikan tombol logout dari route `/pelanggan/` kembali ke gateway root `/index.html`, sama seperti perilaku `pelanggan.html` lama.
TEMUAN        : Pada `pelanggan/script.js` hasil split, `logoutPelanggan()` masih menggunakan `window.location.href = './index.html'`. Karena script berjalan dari `/pelanggan/index.html`, path tersebut menunjuk kembali ke `/pelanggan/index.html`, sehingga logout tidak kembali ke gateway root. Pada `pelanggan.html` lama, `./index.html` memang menunjuk root sehingga perilakunya benar.
PERUBAHAN     : Mengubah hanya target logout pada `pelanggan/script.js` dari `./index.html` menjadi `../index.html`. Tidak mengubah `admin.html`, root `index.html`, atau `pelanggan.html`.
VERIFIKASI    : Source check memastikan fungsi `logoutPelanggan()` sekarang mengarah ke `../index.html`. `node --check pelanggan/script.js` PASS. Path relatif ter-resolve dari `/pelanggan/index.html` menjadi `/index.html`. Perilaku root `pelanggan.html` tidak diubah.
STATUS        : PASS
CATATAN       : Ini adalah koreksi path akibat pemindahan halaman ke subfolder, bukan perubahan alur logout. Uji lokal pengguna sebelumnya sudah mengonfirmasi login/dashboard pada `/pelanggan/` berjalan normal; fokus perbaikan hanya return route setelah logout.

## AUDIT KEMANDIRIAN /pelanggan SEBELUM PENGHAPUSAN pelanggan.html — 2026-09-23

[LOG AKTIVITAS]

Tanggal/Waktu : 2026-09-23
TRACE ID      : `KQ-V3.0.2-PELANGGAN-CLEANUP`
SCOPE         : `pelanggan/` + dependency langsung pelanggan + `readme.md`
AKTIVITAS     : AUDIT / ANALISIS / DOKUMENTASI
FILE TERKAIT  : `pelanggan/index.html`, `pelanggan/style.css`, `pelanggan/script.js`, `manifest-pelanggan.json`, `sw.js`, `pelanggan.html`, `style.css`, `script.js`, `readme.md`
TUJUAN        : Memastikan folder `/pelanggan` benar-benar mandiri dari file legacy sebelum suatu saat `pelanggan.html` dihapus, serta menemukan referensi legacy yang masih akan rusak setelah penghapusan.
TEMUAN        : `pelanggan/index.html` hanya memanggil `style.css` dan `script.js` lokal di foldernya; tidak ditemukan pemanggilan root `style.css`/`script.js`. `pelanggan/style.css` tidak memiliki `@import` atau `url()` relatif yang bergantung pada root. API memakai path root `/api/...`, Firebase memakai CDN, dan service worker memakai `/sw.js`, sehingga ketergantungan tersebut tetap valid dari subfolder. Logout sudah benar menuju `../index.html`. Namun `pelanggan/script.js` masih memiliki fungsi `getPelangganVercelUrl()` yang fallback dan kondisi path-nya menunjuk ke `/pelanggan.html`; fungsi ini dipakai saat membuat QR pelanggan. Jika `pelanggan.html` dihapus tanpa koreksi, QR yang dibuat dari halaman `/pelanggan/` dapat tetap mengarah ke file legacy yang sudah tidak ada. `sw.js` juga masih menyimpan `/pelanggan.html` sebagai rollback/cache dan fallback khusus; ini aman selama rollback dipertahankan, tetapi harus ditinjau saat cutover penghapusan legacy. Komentar `EXTRACTED FROM pelanggan.html` di CSS/JS hanyalah jejak provenance dan bukan dependency runtime.
PERUBAHAN     : Tidak ada perubahan pada kode aplikasi pada audit ini. Hanya `readme.md` yang ditambahkan log audit sesuai aturan proyek.
VERIFIKASI    : Referensi path, manifest, service worker, API, CSS, script, navigasi, dan seluruh kemunculan `pelanggan.html` di folder baru diperiksa langsung. Tidak ditemukan dependency runtime ke root `style.css` atau root `script.js`; satu dependency runtime ke `pelanggan.html` ditemukan pada generator URL QR di `pelanggan/script.js`.
STATUS        : PASS — AUDIT SELESAI / PERBAIKAN LEGACY MASIH DIPERLUKAN
CATATAN       : Sebelum `pelanggan.html` boleh dihapus, minimal perlu memperbaiki generator QR agar canonical URL menjadi `/pelanggan/` (atau URL route pelanggan aktif), kemudian menguji QR. Setelah itu baru audit final service worker/cache untuk menghapus ketergantungan rollback secara terkontrol. Tidak ada perubahan pada `admin.html` atau `index.html`.

## AUDIT AWAL PANEL ADMIN — KQ-V3.0.2-ADMIN-AUDIT

**Tanggal/Waktu:** 2026-09-23  
**TRACE ID:** `KQ-V3.0.2-ADMIN-AUDIT`  
**SCOPE:** `admin.html` + dependensi langsung `script.js` + `style.css`  
**AKTIVITAS:** AUDIT READ-ONLY / NO CHANGE CODE  
**FILE DIPERIKSA:** `admin.html`, `script.js`, `style.css`, `manifest.json`  
**TUJUAN:** Memastikan hubungan root `admin.html` ↔ `script.js` ↔ `style.css` sebelum menentukan apakah panel Admin aman dipisahkan menjadi `/admin/`.  

### TEMUAN
- `admin.html`: 2.555 baris, sekitar 148 KB.
- `admin.html` memuat `style.css?v=1.0.5` dan `script.js` dari root.
- Firebase Compat 10.8.0 dan html5-qrcode dimuat langsung dari CDN.
- Terdapat 6 blok `<style>` dan 9 blok `<script>` di `admin.html`.
- Terdapat 133 inline event handler yang terdeteksi pada audit ini: `onclick` 107, `onchange` 14, `oninput` 8, `onkeydown` 1, `onkeypress` 2, `onsubmit` 1.
- Terdapat 324 inline `style` dan 226 ID; seluruh ID unik.
- Ditemukan 63 deklarasi `function` lokal di `admin.html`, seluruhnya unik.
- `script.js` root memiliki 143 deklarasi `function` dan berisi fungsi-fungsi inti Admin seperti autentikasi, produk, stok, transaksi, pelanggan, catatan, chat, scanner, pengaturan, dan checkout.
- `admin.html` secara nyata memanggil banyak fungsi dari `script.js`; ini mengonfirmasi bahwa `script.js` root adalah pasangan runtime Admin, bukan file pelanggan baru.
- Referensi API Admin yang terdeteksi: `/api/admin-ai`.
- Referensi penyimpanan browser: `localStorage` digunakan; `sessionStorage` tidak terdeteksi.
- Navigasi logout Admin yang terdeteksi mengarah ke `./index.html`.
- `manifest.json` digunakan langsung oleh `admin.html`.
- `sw.js` tidak direferensikan langsung dari markup `admin.html`; registrasi/service-worker perlu ditelusuri dari JavaScript sebelum pemindahan.
- `style.css` memiliki banyak selector yang cocok dengan class Admin; tidak aman memindahkan CSS hanya berdasarkan nama file tanpa audit terhadap inline `<style>` dan cascade.

### KESIMPULAN
**STATUS AUDIT: PASS — NO CHANGE CODE.**

Root `script.js` dan `style.css` memang merupakan pasangan utama panel Admin, dengan `admin.html` sebagai entry point. Namun Admin belum boleh langsung dipindahkan ke `/admin/` karena masih terdapat coupling tinggi antara markup, inline handler, script global, CSS root, manifest, dan kemungkinan service-worker/PWA path.

### KEPUTUSAN TAHAP
Belum membuat folder `/admin/`. Belum memindahkan, menghapus, atau mengubah `admin.html`, `script.js`, `style.css`, `index.html`, atau file PWA.

Tahap berikutnya yang direkomendasikan adalah **audit dependency map Admin** yang lebih rinci: fungsi HTML → `script.js`, inline handler, Firebase initialization, path navigasi, asset/CSS dependency, manifest/service-worker, dan state/localStorage. Setelah dependency map PASS, baru susun blueprint `/admin/` untuk persetujuan sebelum eksekusi split.

## AUDIT DEPENDENCY PANEL ADMIN — 2026-09-23

[LOG AKTIVITAS]

Tanggal/Waktu : 2026-09-23
TRACE ID      : `KQ-V3.0.2-ADMIN-AUDIT`
SCOPE         : `admin.html` + root `script.js` + root `style.css` + dependency langsung Admin + `readme.md`; `index.html` hanya diperiksa sebagai tujuan navigasi, tidak diubah.
AKTIVITAS     : BACA / AUDIT / ANALISIS / DOKUMENTASI
FILE TERKAIT  : `admin.html`, `script.js`, `style.css`, `manifest.json`, `sw.js`, `readme.md`
TUJUAN        : Memetakan dependency nyata Panel Admin sebelum keputusan split ke folder `/admin/`, tanpa mengubah kode aplikasi.
TEMUAN        :
- `admin.html` berukuran 2.555 baris dan menggunakan `style.css?v=1.0.5`, `manifest.json`, ikon root, tiga Firebase Compat CDN, html5-qrcode CDN, serta root `script.js`.
- `admin.html` memiliki 6 blok `<style>` inline dan 5 blok script inline selain 4 script eksternal/CDN/root, sehingga pemisahan tidak boleh hanya memindahkan file HTML secara mentah.
- Root `script.js` memiliki 4.113 baris dan 143 deklarasi fungsi unik; tidak ditemukan nama fungsi duplikat di dalam file tersebut.
- `admin.html` memiliki 133 inline event handler pada audit ini. Banyak handler bersifat kontekstual atau dinamis dan tidak aman dipindahkan massal tanpa klasifikasi.
- `admin.html` memiliki sekitar 324 atribut inline `style`; banyak markup dinamis juga menghasilkan inline style melalui `script.js`/script inline. Ini berarti pemisahan JS dan CSS harus mempertahankan perilaku, bukan sekadar ekstraksi mekanis.
- Salah satu blok script inline utama di `admin.html` menginisialisasi Firebase (`firebase.initializeApp`, `db`, `auth`) dan menetapkan state Admin sebelum `script.js` dimuat. Urutan ini merupakan dependency penting.
- Script inline utama juga berisi fungsi dan listener yang memakai fungsi/state dari `script.js`, sedangkan `script.js` memakai `db`, `auth`, dan state yang dibuat oleh script inline Admin. Dependency bersifat dua arah berdasarkan urutan eksekusi.
- Setelah `script.js` terdapat dua blok inline tambahan: listener badge pelanggan dan migrasi handler H-05-A. Keduanya bergantung pada fungsi/state global dari `script.js` dan harus dipertahankan urutannya jika kelak digabung menjadi satu file.
- Root `style.css` tidak memiliki `@import` dan tidak ditemukan dependency asset relatif CSS yang perlu diubah untuk split dasar. Isi CSS sangat dominan selector Admin, termasuk selector `#admin-*`, `.admin-*`, tab, inventory, chat, scanner, modal, dan tema Admin.
- `manifest.json` menggunakan `start_url: /`, `scope: /`, sehingga belum merupakan manifest khusus route `/admin/`. Ini harus menjadi keputusan terpisah bila nanti Admin benar-benar dipindahkan ke `/admin/` sebagai entry PWA.
- `sw.js` masih mencache `/admin.html` dan `/style.css`, serta fallback navigasi mengenali `/admin.html`. Ini merupakan dependency PWA yang perlu disesuaikan hanya setelah route `/admin/` disetujui.
- Logout Admin pada markup masih mengarah ke `./index.html`; jika halaman dipindahkan ke `/admin/index.html`, target tersebut secara relatif akan menjadi `/admin/index.html` dan karena itu wajib dikoreksi menjadi target root saat split.
- `script.js` juga memiliki fungsi `logout()` yang melakukan `auth.signOut()` tanpa navigasi langsung; alur logout aktual perlu diuji berdasarkan guard/auth listener sebelum perubahan path dilakukan.
- Firebase routing Admin menggunakan `storeCollection()` dan `ACTIVE_TOKO_ID = "toko_v13"`; tidak ditemukan alasan untuk mengubah data-routing hanya karena split folder. Ini harus dipertahankan.

PERUBAHAN    : Tidak ada perubahan pada `admin.html`, `script.js`, `style.css`, `manifest.json`, `sw.js`, atau `index.html`. Hanya `readme.md` yang ditambahkan log audit ini.
VERIFIKASI    : ZIP dibuka dan diperiksa; root file dan ukuran utama terkonfirmasi. Deklarasi fungsi, script/style blocks, external references, Firebase initialization order, storage usage, fetch usage, navigation references, manifest, dan Service Worker references diperiksa langsung dari file aktual. Tidak ada perubahan kode aplikasi.
STATUS        : PASS / NO CHANGE
CATATAN       : Admin **belum boleh langsung di-split**. Dependency utama sudah terpetakan dan secara teknis split sederhana tetap memungkinkan, tetapi harus menggunakan strategi copy-first dengan mempertahankan urutan script inline → Firebase/init → `script.js` → post-script listeners. PWA (`manifest.json`/`sw.js`) dan path logout perlu ditangani sebagai dependency route pada tahap eksekusi, bukan ditebak.

### KEPUTUSAN AUDIT SEMENTARA — ADMIN

Struktur target yang layak untuk dibahas:

`ROOT/admin/index.html`
`ROOT/admin/style.css`
`ROOT/admin/script.js`

File rollback tetap dipertahankan:

`ROOT/admin.html`
`ROOT/style.css`
`ROOT/script.js`

Belum ada folder `/admin/` yang dibuat pada audit ini. Tahap berikutnya adalah **blueprint eksekusi Admin** berdasarkan dependency di atas; belum masuk perubahan kode sampai blueprint disepakati.


---
## H-07-A — FORMALISASI SPLIT ADMIN
**TRACE ID:** KQ-V3.0.2-ADMIN-SPLIT
**SCOPE:** `admin.html` + pasangan root `script.js` / `style.css` + route `/admin/` + `sw.js` + `readme.md`
**WAKTU:** 2026-09-23
**JENIS:** PERUBAHAN TERKONTROL

### Dasar
Hasil local test pengguna membuktikan struktur `/admin/` dengan `index.html`, `script.js`, dan `style.css` dapat berjalan. Perubahan ini memformalkan hasil uji tersebut dengan metode copy-first; file legacy root tetap dipertahankan sebagai rollback.

### Perubahan
- Membuat `/admin/index.html` dari `admin.html`.
- Membuat `/admin/script.js` dari root `script.js`.
- Membuat `/admin/style.css` dari root `style.css`.
- Mengubah manifest reference pada `/admin/index.html` dari `manifest.json` menjadi `../manifest.json`.
- Mengubah logout Admin pada `/admin/index.html` dari `./index.html` menjadi `../index.html` agar kembali ke gateway root.
- Memperbarui `sw.js` cache v8 → v9 dan menambahkan shell `/admin/`, `/admin/index.html`, `/admin/style.css`, `/admin/script.js`.
- Menambahkan fallback navigation untuk `/admin/` dan `/admin/index.html`.
- `admin.html`, root `script.js`, dan root `style.css` TIDAK dihapus.

### Verifikasi
- Source copy Admin dibandingkan dengan baseline sebelum path adjustment.
- Syntax JavaScript: PASS.
- JSON manifest: PASS.
- ZIP integrity: PASS.
- Root legacy rollback files tetap tersedia.
- `pelanggan/` tidak diubah.
- `index.html` gateway tidak diubah.

### Status
**PASS — SPLIT ADMIN TERBENTUK, LEGACY MASIH DIPERTAHANKAN.**

### Catatan
Penghapusan `admin.html`, root `script.js`, dan root `style.css` belum dilakukan. Pensiun legacy hanya setelah `/admin/` lulus local test dan verifikasi deployment.

---
## H-07-B — LEGACY DEPENDENCY CHECK
**TRACE ID:** KQ-V3.0.2-ADMIN-LEGACY-AUDIT
**SCOPE:** referensi `admin.html`, `script.js`, `style.css`, `pelanggan.html` pada project; fokus keputusan pensiun legacy Admin.
**WAKTU:** 2026-09-23
**JENIS:** AUDIT READ-ONLY / NO CODE CHANGE

### Tujuan
Memastikan apakah file legacy root Admin (`admin.html`, `script.js`, `style.css`) sudah dapat dihapus setelah `/admin/` lulus local test, tanpa mengganggu Gateway, Customer, PWA, atau rollback.

### Temuan
- `/admin/index.html`, `/admin/script.js`, dan `/admin/style.css` tersedia dan menjadi pasangan runtime baru.
- `index.html` root masih memiliki navigasi langsung ke `./admin.html`; karena itu `admin.html` **belum boleh dihapus** sebelum tombol/link Gateway dialihkan ke `/admin/`.
- `sw.js` masih mencache `/admin.html` dan `/style.css`, serta masih mengenali `/admin.html` sebagai route fallback. Ini adalah dependency PWA legacy dan belum diubah pada audit ini.
- `admin.html` sendiri masih memuat `style.css` dan `script.js`, sehingga kedua file root masih dibutuhkan jika rollback `admin.html` dipertahankan.
- `pelanggan.html` masih menggunakan root `style.css`; root `style.css` karena itu juga belum aman dipensiunkan selama legacy Customer dipertahankan.
- `pelanggan/` sudah mempunyai `style.css` dan `script.js` sendiri; split Customer tidak memerlukan root Admin assets saat runtime normal.
- `jscheck/s2.js` dan `pelanggan.html`/`pelanggan/script.js` masih memiliki referensi URL legacy `/pelanggan.html`. Ini bukan blocker untuk Admin, dan sesuai keputusan pengguna dibiarkan karena rencana QR Customer nantinya diarahkan ke Gateway root.
- Root `script.js` memiliki pasangan legacy `admin.html`; selama `admin.html` belum dipensiunkan, root `script.js` juga harus dipertahankan.

### Klasifikasi
**🔴 Masih runtime / wajib dipertahankan:**
- `admin.html` — masih menjadi target Gateway root.
- root `script.js` — masih dependency `admin.html`.
- root `style.css` — masih dependency `admin.html` dan `pelanggan.html` legacy.

**🟡 Legacy / PWA rollback:**
- `/admin.html` di `sw.js`.
- `/style.css` di cache Service Worker.

**🟢 Sudah mandiri di route baru:**
- `/admin/index.html` → `/admin/style.css` + `/admin/script.js`.
- `/pelanggan/index.html` → `/pelanggan/style.css` + `/pelanggan/script.js`.

### Perubahan
Tidak ada perubahan pada kode aplikasi, Gateway, Customer, Admin, atau Service Worker. Hanya log audit ini yang ditambahkan ke `readme.md`.

### Verifikasi
- Referensi legacy dicari di seluruh isi ZIP H-07-A.
- `index.html`, `admin.html`, `pelanggan.html`, `sw.js`, `/admin/*`, dan `/pelanggan/*` diperiksa.
- Hasil menunjukkan legacy Admin **belum siap dihapus** pada tahap ini karena Gateway dan rollback/PWA masih menggunakannya.

### Status
**PASS — AUDIT SELESAI / NO CODE CHANGE.**

### Keputusan tahap berikutnya
Jangan hapus `admin.html`, root `script.js`, atau root `style.css` dulu. Tahap aman berikutnya adalah mengalihkan Gateway `index.html` dari `./admin.html` ke `/admin/`, lalu menguji seluruh jalur Gateway → Admin. Setelah itu baru dilakukan audit ulang Service Worker dan dependency legacy sebelum pensiun root Admin.


---
## H-07-C — GATEWAY ADMIN ROUTE MIGRATION
**TRACE ID:** KQ-V3.0.2-ADMIN-ROUTE
**SCOPE:** `index.html` Gateway + `/admin/` route + `readme.md`
**WAKTU:** 2026-09-23
**JENIS:** PERUBAHAN TERKONTROL

### Tujuan
Mengalihkan akses Admin dari file legacy `./admin.html` ke folder runtime baru `./admin/`, berdasarkan hasil local test pengguna yang membuktikan `/admin/` dapat login dan masuk dashboard.

### Temuan
- Gateway root masih menunjuk ke `./admin.html`.
- `/admin/index.html` sudah tersedia dan telah lulus local test pengguna.
- `pelanggan.html` legacy sengaja tidak disentuh.
- Root `admin.html`, `script.js`, dan `style.css` tetap dipertahankan sebagai rollback sampai verifikasi lanjutan selesai.

### Perubahan
- `index.html`: target tombol Admin diubah dari `./admin.html` menjadi `./admin/`.

### Verifikasi
- Referensi Admin pada `index.html` diverifikasi setelah perubahan.
- Tidak ada perubahan pada `admin.html`.
- Tidak ada perubahan pada `pelanggan/`.
- Tidak ada penghapusan file legacy.
- JavaScript inline `index.html` tetap valid secara sintaks.

### Status
**READY FOR LOCAL TEST — GATEWAY → /ADMIN/**

### Catatan
Setelah local test PASS, langkah berikutnya adalah audit ulang seluruh referensi legacy Admin dan evaluasi apakah root `admin.html`, `script.js`, dan `style.css` dapat dipensiunkan.


## H-07-D — Admin Gateway Direct Index Fix
- TRACE ID: KQ-V3.0.2-ADMIN-AUDIT
- SCOPE: `index.html` gateway route to `/admin/`
- DATE: 2026-09-23
- TYPE: diagnosis + repair + verification
- FINDING: Local test of Gateway → Admin returned `FORBIDDEN: No directory listing` when destination was `./admin/`, although `/admin/index.html` itself is present and works.
- CHANGE: Gateway Admin destination changed from `./admin/` to `./admin/index.html` to avoid directory-listing behavior in local/static test servers.
- CUSTOMER: unchanged (`./pelanggan.html` legacy route remains intentionally unchanged).
- ADMIN LEGACY: `admin.html`, root `script.js`, root `style.css` retained as rollback.
- VERIFICATION: target reference count checked; direct `/admin/index.html` exists.
- STATUS: FIXED / READY FOR LOCAL RETEST

## H-07-E — CLEAN ROOT ADMIN LEGACY

[LOG AKTIVITAS]

Tanggal/Waktu : 2026-09-23
TRACE ID      : `KQ-V3.0.2-ADMIN-CLEAN-ROOT`
SCOPE         : Root legacy Admin setelah `/admin/` terbukti berjalan; `admin.html`, root `script.js`, `sw.js`, `readme.md`
AKTIVITAS     : CLEANUP / PENGHAPUSAN LEGACY
FILE TERKAIT  : `admin.html`, `script.js`, `sw.js`, `readme.md`
TUJUAN        : Membersihkan root dari pasangan legacy Admin setelah Admin resmi berpindah ke `/admin/index.html`, `/admin/script.js`, dan `/admin/style.css`.
TEMUAN        : `index.html` sudah mengarah langsung ke `./admin/index.html`. Audit referensi menunjukkan root `script.js` hanya dipakai oleh `admin.html`; root `style.css` masih dipakai oleh `pelanggan.html` legacy sehingga TIDAK dihapus pada tahap ini.
PERUBAHAN     : Menghapus `admin.html` dan root `script.js`. Menghapus `/admin.html` dari precache dan route recognition Service Worker. Cache Service Worker dinaikkan v9 → v10. Root `style.css` dipertahankan sebagai dependency rollback/legacy `pelanggan.html`.
VERIFIKASI    : Tidak ditemukan referensi runtime aktif ke `admin.html` atau root `script.js`. `/admin/index.html` tetap memiliki pasangan `/admin/script.js` dan `/admin/style.css`. `index.html` tetap mengarah ke `/admin/index.html`. `pelanggan.html` tetap memiliki akses ke root `style.css`, sehingga file tersebut dipertahankan.
STATUS        : PASS — CLEANUP ADMIN ROOT SELESAI, CUSTOMER LEGACY BELUM DIPENSIUNKAN.
CATATAN       : `pelanggan.html` dan root `style.css` sengaja belum dihapus karena masih berada dalam rencana migrasi berikutnya. `admin.html` dan root `script.js` sudah dipensiunkan dari root aktif.

### AUDIT ROOT CLEANUP — H-07-F — 2026-09-23

[LOG AKTIVITAS]

Tanggal/Waktu : 2026-09-23
TRACE ID      : `KQ-V3.0.2-ROOT-CLEANUP`
SCOPE         : Root project setelah H-07-E; fokus `pelanggan.html`, `style.css`, `check*.js`, `jscheck/`, dan dependensi runtime terkait.
AKTIVITAS     : BACA / AUDIT / ANALISIS
FILE TERKAIT  : `index.html`, `pelanggan.html`, `style.css`, `sw.js`, `check0.js`, `check1.js`, `check2.js`, `jscheck/s1.js`–`jscheck/s6.js`, `/pelanggan/*`, `/admin/*`, `readme.md`
TUJUAN        : Menentukan kandidat file root yang dapat dibersihkan setelah Admin dan Customer baru terbukti berjalan lokal.
TEMUAN        : `/admin/` dan `/pelanggan/` sudah memiliki pasangan asset masing-masing. `pelanggan.html` masih menjadi legacy/rollback dan masih memakai root `style.css`; karena itu keduanya belum aman dihapus. `jscheck/s1.js`–`s6.js` dan `check0.js`–`check2.js` tampak sebagai artefak pemeriksaan/audit dan tidak ditemukan sebagai dependency runtime dari aplikasi utama pada pemeriksaan referensi root. `jscheck/s2.js` masih menyimpan fallback URL lama `/pelanggan.html`, sehingga artefak audit tersebut tidak boleh disamakan dengan dependency runtime aplikasi baru.
PERUBAHAN     : Tidak ada perubahan kode aplikasi. Hanya pencatatan audit ini.
VERIFIKASI    : Struktur root dan referensi runtime diperiksa ulang. Status kandidat cleanup dipisahkan antara runtime/rollback dan artefak audit.
STATUS        : NO CHANGE
CATATAN       : Rekomendasi tahap berikutnya: bersihkan artefak audit/check terlebih dahulu setelah memastikan tidak diperlukan untuk tooling aktif; pertahankan `pelanggan.html` + root `style.css` sementara sampai migrasi QR → Gateway selesai; setelah itu lakukan final legacy removal.

[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23 10:00 WIB
TRACE ID      : KQ-V3.0.2-H07F-ROOT-CLEANUP
SCOPE         : Artefak audit root `check0.js`, `check1.js`, `check2.js`, dan folder `jscheck/`.
AKTIVITAS     : HAPUS + VERIFIKASI
FILE TERKAIT  : `check0.js`, `check1.js`, `check2.js`, `jscheck/s1.js` s.d. `jscheck/s6.js`, `readme.md`.
TUJUAN        : Membersihkan artefak pemeriksaan/eksperimen yang tidak memiliki referensi runtime dari root aplikasi.
TEMUAN        : Pencarian seluruh proyek tidak menemukan referensi runtime ke `check0.js`, `check1.js`, `check2.js`, atau `jscheck/`. File-file tersebut berisi potongan/prototipe hasil pemeriksaan, bukan entry point aplikasi.
PERUBAHAN     : Menghapus 3 file `check*.js` dan 6 file di `jscheck/` beserta folder `jscheck/` yang menjadi kosong.
VERIFIKASI    : Referensi ulang ke seluruh proyek = tidak ditemukan. Struktur root diperiksa ulang. Pemeriksaan sintaks JavaScript tersisa dilakukan setelah penghapusan.
STATUS        : PASS
CATATAN       : `pelanggan.html` dan root `style.css` sengaja TIDAK dihapus karena masih menjadi legacy/rollback Customer sesuai rencana migrasi Gateway. Admin sudah berada di `/admin/`.

## H-07-G — AUDIT FINAL DEPENDENCY `pelanggan.html` ↔ ROOT `style.css`

[LOG AKTIVITAS]

Tanggal/Waktu : 2026-09-23 10:00 WIB
TRACE ID      : `KQ-V3.0.2-H07G-CUSTOMER-LEGACY-AUDIT`
SCOPE         : `pelanggan.html`, root `style.css`, route Customer pada `index.html`, `pelanggan/`, `sw.js`, `readme.md`
AKTIVITAS     : BACA / AUDIT / ANALISIS / VERIFIKASI
FILE TERKAIT  : `pelanggan.html`, `style.css`, `index.html`, `pelanggan/index.html`, `pelanggan/style.css`, `pelanggan/script.js`, `sw.js`, `readme.md`
TUJUAN        : Menentukan apakah `pelanggan.html` dan root `style.css` sudah aman dipensiunkan setelah Customer memiliki runtime baru di `/pelanggan/`.
TEMUAN        : `index.html` masih mengarahkan jalur Customer ke `./pelanggan.html`. `pelanggan.html` masih memuat root `style.css`. `sw.js` masih memiliki precache `/pelanggan.html` dan `/style.css` serta fallback navigasi `/pelanggan.html`. `/pelanggan/index.html` sudah memakai `/pelanggan/style.css` dan `/pelanggan/script.js`, sehingga runtime Customer baru tidak membutuhkan root `style.css` secara langsung. `pelanggan.html` dan `pelanggan/script.js` juga masih memiliki fallback URL `/pelanggan.html`; referensi ini dipertahankan sesuai keputusan bahwa jalur QR/Customer legacy belum dimigrasikan dan nantinya akan diarahkan melalui Gateway root.
PERUBAHAN     : Tidak ada file aplikasi yang dihapus, dipindahkan, atau diubah pada audit ini. Hanya `readme.md` yang ditambahkan log audit.
VERIFIKASI    : Pencarian referensi proyek menunjukkan dependency aktif/legacy yang masih nyata pada `index.html`, `pelanggan.html`, dan `sw.js`. Tidak ditemukan kebutuhan root `style.css` dari `/pelanggan/` runtime baru. Hasil audit konsisten dengan struktur copy-first yang sedang berjalan.
STATUS        : NO CHANGE
CATATAN       : `pelanggan.html` + root `style.css` BELUM aman untuk dipensiunkan pada tahap ini karena Gateway dan Service Worker masih mengenali jalur legacy Customer. Tahap berikutnya harus berupa keputusan/migrasi route Customer dari Gateway/QR ke `/pelanggan/` atau Gateway root terlebih dahulu, lalu audit Service Worker dan final removal. Jangan hapus kedua file ini sebelum migrasi tersebut lulus local test.


## H-07-H — MIGRASI GATEWAY CUSTOMER KE `/pelanggan/`

[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23 10:20 WIB
TRACE ID      : KQ-V3.0.2-H07H-CUSTOMER-ROUTE
SCOPE         : `index.html`, `sw.js`, `readme.md`; target runtime Customer `/pelanggan/`.
AKTIVITAS     : PERUBAHAN ROUTE + PEMBERSIHAN DEPENDENCY PWA LEGACY + VERIFIKASI
FILE TERKAIT  : `index.html`, `sw.js`, `readme.md`
TUJUAN        : Memindahkan jalur Customer Gateway dari `./pelanggan.html` ke runtime baru `./pelanggan/index.html` agar legacy Customer tidak lagi menjadi entry point utama.
TEMUAN        : Audit H-07-G membuktikan `/pelanggan/` sudah memiliki `index.html`, `style.css`, dan `script.js` mandiri. Gateway sebelumnya masih menunjuk `./pelanggan.html`. Service Worker masih melakukan precache dan route recognition terhadap `/pelanggan.html` serta root `/style.css`.
PERUBAHAN     : `index.html` sekarang mengarahkan Customer ke `./pelanggan/index.html`. `sw.js` dinaikkan v10 → v11, menghapus `/pelanggan.html` dan root `/style.css` dari precache, serta menghapus `/pelanggan.html` dari daftar route Customer aktif. `pelanggan.html` dan root `style.css` BELUM dihapus sebagai rollback sampai local test PASS.
VERIFIKASI    : Referensi Gateway Customer baru ditemukan pada `./pelanggan/index.html`. Runtime `/pelanggan/` tetap memiliki pasangan asset sendiri. Syntax check JavaScript dilakukan setelah perubahan.
STATUS        : SIAP LOCAL TEST
CATATAN       : Ini adalah tahap migrasi route, bukan final deletion. Setelah local test Gateway → Customer PASS, dilakukan audit terakhir dan baru diputuskan penghapusan `pelanggan.html` + root `style.css`.

## H-07-I — LOCAL + ONLINE TEST CUSTOMER `PENGATURAN → BAGIKAN APLIKASI`

[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23
TRACE ID      : `KQ-V3.0.2-H07I-CUSTOMER-SHARE-TEST`
SCOPE         : Fitur Customer `Pengaturan → Bagikan Aplikasi`, termasuk `Lihat code barcode` dan `Bagikan ke WA`.
AKTIVITAS     : TEST LOKAL + TEST ONLINE / OBSERVASI
FILE TERKAIT  : Runtime `/pelanggan/*` dan dependensi fitur share/barcode terkait.
TUJUAN        : Memastikan migrasi Customer H-07-H tidak merusak fitur berbagi aplikasi pada runtime baru.
HASIL LOCAL   : `Lihat code barcode` membuka popup/floating barcode. Saat menjalankan aksi `Bagikan Aplikasi` setelah popup tersebut, UI terindikasi hang/berat; dugaan awal adalah proses pembuatan/render barcode yang membebani proses, tetapi penyebab belum ditetapkan tanpa audit kode lebih lanjut. `Bagikan ke WA` berjalan mulus dan mengarahkan ke halaman/pop-up WhatsApp.
HASIL ONLINE  : Kedua alur (`Lihat code barcode` dan `Bagikan ke WA`) teruji normal pada deployment online.
PERUBAHAN     : Tidak ada perubahan kode pada tahap pengujian ini.
VERIFIKASI    : Alur Customer utama tetap berjalan setelah H-07-H. Perbedaan perilaku barcode hanya teramati pada local test; online dilaporkan normal.
STATUS        : PASS UNTUK ROUTE/MIGRASI; CATATAN PERFORMANCE BARCODE
CATATAN       : Jangan mengubah implementasi barcode hanya berdasarkan dugaan. Jika optimasi diperlukan, lakukan audit khusus proses generate/render barcode terlebih dahulu, lalu uji ulang local dan online. Fitur `Bagikan ke WA` tidak memerlukan perubahan berdasarkan hasil tes ini.

## H-07-J — FINAL CUSTOMER LEGACY REMOVAL / MIGRASI STEP

[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23
TRACE ID      : `KQ-V3.0.2-H07J-CUSTOMER-LEGACY-REMOVAL`
SCOPE         : Finalisasi migrasi Customer dari root legacy ke `/pelanggan/`.
AKTIVITAS     : PERUBAHAN / HAPUS / VERIFIKASI / DOKUMENTASI
FILE TERKAIT  : `pelanggan/script.js`, `pelanggan.html`, root `style.css`, `readme.md`
TUJUAN        : Menutup jalur legacy Customer setelah H-07-H route migration dan H-07-I local + online testing dinyatakan PASS.
TEMUAN        : `/pelanggan/` sudah memiliki runtime mandiri (`index.html`, `style.css`, `script.js`). Gateway sudah mengarah ke `./pelanggan/index.html`. Pengujian silang lokal/online membuktikan runtime baru berjalan; hang barcode/share hanya terjadi pada browser tertentu dan tidak muncul pada browser lain/online, sehingga tidak diperlakukan sebagai bug aplikasi.
PERUBAHAN     : Menghapus root `pelanggan.html` dan root `style.css`. Memperbarui fallback URL pada `pelanggan/script.js` dari `/pelanggan.html` menjadi `/pelanggan/` agar fitur share/QR tidak lagi menghasilkan target legacy.
VERIFIKASI    : Tidak ada file runtime yang tersisa dengan referensi ke `pelanggan.html`. `/pelanggan/index.html` tetap memakai `style.css` dan `script.js` lokal. Gateway tetap mengarah ke `/pelanggan/index.html`. JavaScript syntax check dan ZIP integrity dilakukan setelah perubahan.
STATUS        : PASS — CUSTOMER LEGACY ROOT DIPENSIUNKAN.
CATATAN       : QR/Share sekarang mengarah ke runtime `/pelanggan/`. Migrasi berikutnya dapat melanjutkan pengembangan Gateway/QR tanpa ketergantungan pada `pelanggan.html`.

### LOG AKTIVITAS — H-07-K — AUDIT & MIGRASI MANIFEST PELANGGAN — 2026-09-23

[LOG AKTIVITAS]

Tanggal/Waktu : 2026-09-23
TRACE ID      : `KQ-V3.0.3-H07-K-MANIFEST-PELANGGAN`
SCOPE         : `manifest-pelanggan.json` + `pelanggan/index.html` + `sw.js` + `manifest.json` + `readme.md`
AKTIVITAS     : AUDIT → UBAH → HAPUS → VERIFIKASI
FILE TERKAIT  : `manifest-pelanggan.json`, `pelanggan/index.html`, `sw.js`, `manifest.json`, `readme.md`
TUJUAN        : Mengakhiri ketergantungan manifest standalone pelanggan setelah `/pelanggan/` menjadi bagian dari aplikasi utama.
TEMUAN        : `manifest-pelanggan.json` masih aktif direferensikan oleh `pelanggan/index.html` dan `sw.js`. Manifest tersebut memiliki identitas/start_url khusus standalone pelanggan, sedangkan `manifest.json` utama sudah menjadi manifest aplikasi dengan scope `/` dan start_url `/`.
PERUBAHAN    : `pelanggan/index.html` dialihkan dari `manifest-pelanggan.json` ke `manifest.json`; referensi serta precache `manifest-pelanggan.json` di `sw.js` dihapus; rule static-cache khusus `manifest-pelanggan.json` dihapus; file `manifest-pelanggan.json` dihapus. `manifest.json` utama dipertahankan tanpa perubahan.
VERIFIKASI    : Referensi `manifest-pelanggan.json` = 0 pada source aktif; `pelanggan/index.html` memakai `../manifest.json`; `sw.js` tidak lagi memuat nama manifest standalone; `manifest.json` utama tetap valid JSON; source JavaScript terkait tetap dapat diperiksa tanpa error sintaks.
STATUS        : PASS
CATATAN       : Keputusan ini mengonsolidasikan PWA pelanggan ke identitas aplikasi utama. Tidak ada perubahan pada icon identitas aplikasi.


### LOG AKTIVITAS — H-07-L — KONSOLIDASI MANIFEST KE GATEWAY ROOT — 2026-09-23

[LOG AKTIVITAS]

Tanggal/Waktu : 2026-09-23
TRACE ID      : `KQ-V3.0.3-H07-L-MANIFEST-ROOT`
SCOPE         : `manifest.json`, `index.html`, `admin/index.html`, `pelanggan/index.html`, `sw.js`, `readme.md`
AKTIVITAS     : AUDIT → PEMBERSIHAN REFERENSI → VERIFIKASI
TUJUAN        : Menetapkan `manifest.json` sebagai manifest tunggal aplikasi pada Gateway/root, bukan manifest per modul.
TEMUAN        : `manifest.json` utama valid dan memakai scope `/` serta start_url `/`. Gateway `index.html` adalah entry point utama dan tetap menjadi pemilik manifest. `admin/index.html` dan `pelanggan/index.html` sebelumnya masih mendeklarasikan manifest utama; Admin juga memiliki referensi `icon-192.png` relatif yang tidak tersedia di folder `/admin/`.
PERUBAHAN    : Menghapus deklarasi `<link rel="manifest">` dan `<link rel="apple-touch-icon">` dari `admin/index.html`; menghapus deklarasi manifest dari `pelanggan/index.html`. `manifest.json` root, `index.html` Gateway, icon identitas, dan `sw.js` tidak diubah.
VERIFIKASI    : Hanya `index.html` yang mendeklarasikan manifest. Referensi `manifest-pelanggan.json` = 0. `admin/index.html` dan `pelanggan/index.html` tidak lagi mendeklarasikan manifest/icon relatif. `manifest.json` tetap valid JSON dan `sw.js` tetap melakukan precache manifest + icon identitas utama.
STATUS        : PASS
CATATAN       : Struktur PWA sekarang dikonsolidasikan: Gateway/root = pemilik manifest dan identitas aplikasi; `/admin/` dan `/pelanggan/` = modul runtime tanpa manifest sendiri. Icon `icon-192.png` dan `icon-512.png` tetap berada di ROOT sebagai aset identitas aplikasi.


### LOG AKTIVITAS — H-07-O — PEMINDAHAN ASSET GATEWAY KE FOLDER ASSETS — 2026-09-23

[LOG AKTIVITAS]

Tanggal/Waktu : 2026-09-23
TRACE ID      : `KQ-V3.0.3-H07-O-GATEWAY-ASSETS`
SCOPE         : `index.html`, `sw.js`, `slide1.png`, `slide2.png`, `slide3.png`, `robot-head-ai.png`, `assets/gateway/`, `readme.md`
AKTIVITAS     : PEMINDAHAN ASSET → UPDATE PATH → VERIFIKASI
TUJUAN        : Merapikan asset visual Gateway ke folder `assets/gateway/` tanpa mengubah icon identitas PWA.
TEMUAN        : Empat PNG (`slide1.png`, `slide2.png`, `slide3.png`, `robot-head-ai.png`) merupakan asset visual Gateway/index.html. Icon `icon-192.png` dan `icon-512.png` tetap merupakan identitas PWA di ROOT dan tidak dipindahkan.
PERUBAHAN    : Memindahkan empat PNG Gateway ke `assets/gateway/`. Memperbarui referensi gambar dan array slide pada `index.html`. Memperbarui precache dan rule static-cache `slide[123].png` pada `sw.js` agar membaca path `/assets/gateway/`.
VERIFIKASI    : Keempat PNG berada di `assets/gateway/`; referensi lama `./slide1.png`, `./slide2.png`, `./slide3.png`, `./robot-head-ai.png` pada `index.html` = 0; path baru terdeteksi; icon PWA tetap di ROOT; struktur ZIP valid.
STATUS        : PASS
CATATAN       : Pemindahan hanya berlaku untuk asset visual Gateway. Tidak ada perubahan pada `manifest.json` atau icon identitas PWA.


### LOG AKTIVITAS — H-07-P — README FIRST FOLDER — 2026-09-23
[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23
TRACE ID      : `KQ-V3.0.3-H07-P-README-FIRST`
SCOPE         : `readme.md`, `README FIRST/`, ZIP project
AKTIVITAS     : PEMINDAHAN DOKUMENTASI
TUJUAN        : Menempatkan `readme.md` ke folder `README FIRST/` sebagai lokasi dokumentasi utama tanpa mengubah file aplikasi lainnya.
TEMUAN        : README sebelumnya berada di `h07j_work/readme.md`.
PERUBAHAN     : `h07j_work/readme.md` dipindahkan menjadi `h07j_work/README FIRST/readme.md`.
VERIFIKASI    : Seluruh isi proyek selain lokasi README dipertahankan; README hanya memiliki satu lokasi baru; struktur ZIP valid.
STATUS        : PASS
CATATAN       : Tidak ada perubahan pada Gateway, Admin, Pelanggan, PWA identity, asset, API, atau file runtime lainnya.


### LOG AKTIVITAS — H-07-Q — CUSTOMER LOGIN-FIRST / WELCOME REMOVAL — 2026-09-23

[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23
TRACE ID      : `KQ-V3.0.3-H07-Q-CUSTOMER-LOGIN-FIRST`
SCOPE         : `pelanggan/index.html`, `pelanggan/script.js`, `pelanggan/style.css`, `README FIRST/readme.md`
AKTIVITAS     : PEMBERSIHAN LEGACY → PENYESUAIAN ALUR LOGIN → VERIFIKASI
TUJUAN        : Menghapus Welcome Screen pelanggan lama dan menjadikan Login Modal sebagai pintu masuk utama Customer.
TEMUAN        : Welcome Screen pelanggan masih menjadi lapisan pembuka lama dan memiliki dependency JavaScript untuk hide/show, store name, tombol install, serta transisi ke login. Mekanisme PWA install sudah tersedia dan dapat dipertahankan tanpa Welcome Screen.
PERUBAHAN     : Menghapus Welcome Screen beserta seluruh dependency `welcomeScreen`, `welcomeStoreName`, `welcomeInstallBtn`, `bukaLoginDariWelcome()`, `tutupWelcomeScreen()`, dan pengecekan standalone khusus Welcome. Login Modal kini tampil langsung untuk pelanggan yang belum login. Fitur `Install Aplikasi KasirQuh` dipindahkan ke bawah area Login/Daftar menggunakan mekanisme `beforeinstallprompt` yang sama. Card install lama di Pengaturan dihapus agar tidak duplikat.
VERIFIKASI    : Referensi Welcome pada `pelanggan/` = 0. Referensi install aktif hanya melalui `customerLoginInstallCard` + `triggerInstallPWA()`. JavaScript `pelanggan/script.js` lolos `node --check`. PWA root, Gateway, Admin, Firebase schema, manifest, service worker, dan icon identitas tidak diubah.
STATUS        : PASS — CUSTOMER LOGIN-FIRST CLEANUP
CATATAN       : Setelah logout, sesi Customer dibersihkan dan halaman pelanggan di-reload sehingga tetap kembali ke Login Modal; tidak lagi diarahkan ke root Gateway atau Welcome Screen pelanggan.


### LOG AKTIVITAS — H-07-R — CUSTOMER PWA INSTALL PROMPT ROOT MANIFEST FIX — 2026-09-23

[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23
TRACE ID      : `KQ-V3.0.3-H07-R-CUSTOMER-PWA-INSTALL-ROOT-MANIFEST`
SCOPE         : `pelanggan/index.html`, `pelanggan/script.js`, `manifest.json`, `README FIRST/readme.md`
AKTIVITAS     : AUDIT REFERENSI H06E → PERBAIKAN TERARAH → VERIFIKASI
TUJUAN        : Mengembalikan native install prompt Customer tanpa menghidupkan kembali manifest pelanggan terpisah.
TEMUAN        : H06E yang diuji pengguna berhasil menampilkan native prompt “Instal aplikasi KasirQuh pelanggan”. Pada H-07-Q, `pelanggan/index.html` tidak lagi memiliki deklarasi `<link rel="manifest">`, sementara `pelanggan/script.js` tetap bergantung pada event `beforeinstallprompt`. Akibatnya `deferredPrompt` dapat tetap `null` dan tombol jatuh ke fallback menu Chrome.
PERUBAHAN     : Menambahkan satu deklarasi `<link href="/manifest.json" rel="manifest">` pada `pelanggan/index.html` untuk menghubungkan modul Customer ke manifest aplikasi root yang sudah dikonsolidasikan. Tidak menghidupkan kembali `manifest-pelanggan.json`.
FILE BERUBAH : `h07j_work/pelanggan/index.html`, `h07j_work/README FIRST/readme.md`
FILE TIDAK BERUBAH : `manifest.json`, `sw.js`, icon PWA, Gateway, Admin, Firebase, `pelanggan/script.js`, `pelanggan/style.css`.
VERIFIKASI    : Link manifest Customer = 1 dan menunjuk `/manifest.json`; `manifest-pelanggan.json` tidak direferensikan oleh file runtime aktif; riwayat penyebutan lama pada dokumentasi dipertahankan sebagai arsip; `beforeinstallprompt` dan `triggerInstallPWA()` tetap utuh; JavaScript `pelanggan/script.js` lolos `node --check`; struktur ZIP dan isi file diverifikasi setelah packaging.
STATUS        : PASS — ROOT MANIFEST LINK RESTORED FOR CUSTOMER INSTALL PROMPT
CATATAN       : Ini adalah fix minimal berdasarkan perbedaan nyata H06E → H-07-Q. Tidak ada fallback patch tambahan dan tidak ada perubahan pada identitas PWA root.

### LOG AKTIVITAS — H-07-S — GATEWAY PWA INSTALL HUB — 2026-09-23

[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23
TRACE ID      : `KQ-V3.0.3-H07-S-GATEWAY-PWA-INSTALL-HUB`
SCOPE         : `index.html`, `pelanggan/script.js`, `README FIRST/readme.md`
AKTIVITAS     : PEMUSATAN INSTALL PWA DI GATEWAY
TUJUAN        : Menjadikan Gateway (`/index.html`) sebagai satu-satunya entry point tombol native PWA install karena hasil uji pengguna menunjukkan instalasi native Chrome berhasil dari Gateway, sedangkan `/pelanggan/` dan `/admin/` menghasilkan shortcut.
TEMUAN        : Gateway menggunakan root manifest dan merupakan entry point yang terbukti dapat di-install sebagai PWA native melalui Chrome. Customer sebelumnya menangkap `beforeinstallprompt` sendiri dan mencoba meng-install `/pelanggan/`, sehingga hasilnya dapat berupa shortcut.
PERUBAHAN     : Menambahkan tombol floating `📲 Install` di atas slide Gateway. Tombol hanya tampil ketika Chrome memberikan `beforeinstallprompt`, lalu memanggil native install prompt. Pada Customer, mekanisme `beforeinstallprompt` lokal dihapus; kartu/tombol Install tetap terlihat dan diarahkan ke Gateway dengan query `?install=1`. Manifest root, service worker, icon PWA, slider PNG, tombol Mulai Belanja, dan tombol Admin tidak diubah.
FILE BERUBAH : `h07j_work/index.html`, `h07j_work/pelanggan/script.js`, `h07j_work/README FIRST/readme.md`
FILE TIDAK BERUBAH : `manifest.json`, `sw.js`, icon PWA, `assets/gateway/*.png`, `pelanggan/index.html`, `pelanggan/style.css`, Admin runtime, Firebase.
VERIFIKASI : Struktur tombol install Gateway menggunakan event `beforeinstallprompt`; Customer tidak lagi memiliki handler `beforeinstallprompt` atau `deferredPrompt` aktif; fungsi Customer hanya mengarahkan ke root Gateway; JavaScript Customer lolos `node --check`; HTML Gateway memiliki satu tombol install floating dan slider tetap menggunakan PNG yang sama.
STATUS        : PASS — GATEWAY DIJADIKAN PUSAT INSTALL PWA
CATATAN       : Query `?install=1` menjadi penanda konteks dari Customer menuju Gateway. Pada tahap ini Gateway belum memaksa prompt otomatis; prompt hanya dipanggil melalui tombol floating setelah Chrome menyediakan event install.


### LOG AKTIVITAS — H-07-T — CUSTOMER PWA INSTALL REMOVAL — 2026-09-23

[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23
TRACE ID      : `KQ-V3.0.3-H07-T-CUSTOMER-PWA-INSTALL-REMOVAL`
SCOPE         : `pelanggan/index.html`, `pelanggan/script.js`, `pelanggan/style.css`, `README FIRST/readme.md`
AKTIVITAS     : AUDIT → PEMBERSIHAN ENTRY INSTALL CUSTOMER → VERIFIKASI
TUJUAN        : Menghapus seluruh UI dan handler instalasi PWA dari modul Customer karena instalasi native telah dipusatkan dan terbukti berhasil melalui Gateway.
TEMUAN        : `/pelanggan/` masih memiliki kartu/tombol Install, fungsi `triggerInstallPWA()`, dan deklarasi manifest root yang tidak diperlukan untuk modul Customer. Service worker tetap dibutuhkan sebagai runtime/cache aplikasi dan bukan sebagai tombol install.
PERUBAHAN     : Menghapus link manifest dari `pelanggan/index.html`, menghapus kartu/tombol Install Customer, menghapus fungsi `triggerInstallPWA()`, dan menghapus aturan CSS khusus `#customerLoginInstallCard`. Tidak mengubah service worker registration, login/register, katalog, Firebase, Gateway, Admin, manifest root, icon PWA, atau asset Gateway.
FILE BERUBAH : `h07j_work/pelanggan/index.html`, `h07j_work/pelanggan/script.js`, `h07j_work/pelanggan/style.css`, `h07j_work/README FIRST/readme.md`
FILE TIDAK BERUBAH : `manifest.json`, `sw.js`, icon PWA, `index.html` Gateway, `assets/gateway/*.png`, Admin runtime, Firebase.
VERIFIKASI    : Referensi install/manifest pada runtime `pelanggan/` = 0; `triggerInstallPWA`, `customerLoginInstallCard`, `card-install-pwa`, `beforeinstallprompt`, dan `deferredPrompt` = 0; service worker registration Customer tetap ada; JavaScript Customer lolos `node --check`; struktur ZIP valid.
STATUS        : PASS — CUSTOMER BUKAN ENTRY INSTALL PWA
CATATAN       : Instalasi PWA hanya melalui Gateway. `/pelanggan/` tetap menjadi modul belanja/login biasa dan tidak menawarkan instalasi PWA maupun shortcut.


### LOG AKTIVITAS — H-07-U — CUSTOMER LOGOUT → GATEWAY — 2026-09-23

[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23
TRACE ID      : `KQ-V3.0.3-H07-U-CUSTOMER-LOGOUT-GATEWAY`
SCOPE         : `pelanggan/script.js`, `README FIRST/readme.md`
AKTIVITAS     : PERBAIKAN CUSTOMER LOGOUT
TUJUAN        : Mengarahkan logout Customer langsung kembali ke Gateway root `/` tanpa mengubah jalur Admin atau mekanisme PWA Gateway.
TEMUAN        : `logoutPelanggan()` sebelumnya menghapus sesi Customer lalu membuka kembali login modal dan `window.location.reload()`, sehingga pengguna tetap berada di `/pelanggan/`.
PERUBAHAN     : Setelah sesi Customer dihapus, fungsi sekarang menjalankan `window.location.href = '/'` agar logout kembali ke Gateway.
FILE BERUBAH : `h07j_work/pelanggan/script.js`, `h07j_work/README FIRST/readme.md`
FILE TIDAK BERUBAH : `admin/`, `index.html` Gateway, `manifest.json`, `sw.js`, Customer HTML/CSS, Firebase, API, dan asset Gateway.
VERIFIKASI    : Fungsi logout Customer hanya menghapus sesi lalu redirect ke `/`; tidak lagi reload/login-modal. JavaScript lulus `node --check`; ZIP dipaketkan ulang dan isi proyek diverifikasi.
STATUS        : PASS
CATATAN       : Scope sengaja dibatasi hanya Customer Logout. PWA Customer tetap tanpa install UI/logic; Gateway tetap menjadi satu-satunya hub instalasi PWA.


### LOG AKTIVITAS — H-07-V — CUSTOMER DESKTOP RESPONSIVE VIEW — 2026-09-23

[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23
TRACE ID      : `KQ-V3.0.3-H07-V-CUSTOMER-DESKTOP-RESPONSIVE`
SCOPE         : `pelanggan/style.css`, `README FIRST/readme.md`
AKTIVITAS     : PERBAIKAN VIEW DESKTOP CUSTOMER
TUJUAN        : Memisahkan canvas Gateway yang tetap terkontrol dari layout Customer agar `/pelanggan/` tidak lagi dipaksa selebar 430px pada layar desktop.
TEMUAN        : CSS Customer memiliki blok `DESKTOP PORTRAIT VIEWPORT V1` yang mengunci `body`, bottom navigation, modal, FAB, dan toast ke lebar maksimum 430px pada viewport >= 768px. Fondasi responsive Customer sendiri sudah memiliki media query 700px/1200px dan grid katalog yang dapat memanfaatkan ruang layar.
PERUBAHAN     : Mengganti blok penguncian portrait 430px menjadi aturan desktop responsive >= 768px: `body` mengikuti lebar viewport, navigasi menjadi full-width, FAB kembali ke sisi kanan viewport, modal mengikuti viewport, dan toast tetap terpusat dengan batas wajar. Tampilan mobile < 768px tidak diubah.
FILE BERUBAH : `h07j_work/pelanggan/style.css`, `h07j_work/README FIRST/readme.md`
FILE TIDAK BERUBAH : Gateway `index.html`, Slide 1/2/3, hotspot Gateway, `manifest.json`, `sw.js`, icon PWA, `pelanggan/index.html`, `pelanggan/script.js`, Firebase/DB, `ACTIVE_TOKO_ID`, `cust_*`, dan seluruh `admin/`.
VERIFIKASI    : Blok `width/min-width/max-width: 430px` pada desktop Customer dihapus; aturan responsive 700px/1200px tetap ada; hanya dua file proyek berubah; struktur ZIP diverifikasi; JavaScript Customer tetap berasal dari H-07-U tanpa perubahan.
STATUS        : PASS — CUSTOMER DESKTOP VIEW UNLOCKED
CATATAN       : Gateway sengaja tetap menggunakan canvas portrait untuk menjaga Slide 1/2/3 dan posisi tombol. Perubahan ini hanya berlaku untuk `/pelanggan/` pada viewport >= 768px.

### LOG AKTIVITAS — H-07-W — ADMIN DESKTOP RESPONSIVE / ROOT UNLOCK — 2026-09-23

[LOG AKTIVITAS]
Tanggal/Waktu : 2026-09-23
TRACE ID      : `KQ-V3.0.3-H07-W-ADMIN-DESKTOP-RESPONSIVE`
SCOPE         : `admin/index.html`, `README FIRST/readme.md`
AKTIVITAS     : AUDIT → PEMBERSIHAN DESKTOP FORCED VIEW → VERIFIKASI
TUJUAN        : Mengembalikan layout Admin desktop ke fondasi responsive yang sudah tersedia tanpa membuat tambalan CSS baru.
TEMUAN        : `admin/index.html` memiliki blok `admin-desktop-portrait-viewport-v1` yang pada viewport >= 768px mengunci `body`, main content, modal/backdrop, bottom bar, FAB, AI, scanner, opening/welcome, dan toast ke frame 430px. CSS dasar Admin sendiri sudah menyediakan `body` full viewport, `.main-content` fleksibel, modal fixed full viewport, FAB fixed viewport, dan scanner `100vw × 100vh`.
PERUBAHAN     : Menghapus satu blok forced desktop portrait tersebut secara utuh. Tidak mengganti dengan `!important` baru. Dengan demikian desktop kembali memakai aturan dasar responsive; mobile tidak disentuh.
FILE BERUBAH : `admin/index.html`, `README FIRST/readme.md`
FILE TIDAK BERUBAH : `admin/script.js`, `admin/style.css`, `pelanggan/`, Gateway, Firebase/DB, API, manifest, service worker, dan asset Gateway.
VERIFIKASI    : Marker `admin-desktop-portrait-viewport-v1` = 0; referensi `430px` pada `admin/index.html` = 0; CSS dasar `.modal-backdrop`, `.fab-scanner-modal`, `.scanner-fullscreen-box`, `.fab-container`, `.permanent-bottom-bar`, dan `.main-content` tetap tersedia; struktur ZIP valid.
STATUS        : PASS — ADMIN DESKTOP ROOT UNLOCKED
CATATAN       : Ini adalah tahap akar masalah. POS, modal, bottom bar/FAB, AI, dan scanner selanjutnya diaudit sebagai komponen individual tanpa mengubah logic JavaScript. Scanner tetap menggunakan layout fullscreen `100vw × 100vh` dari `admin/style.css`.



### LOG AKTIVITAS — H-07-X — POS CART MODAL RESPONSIVE — 2026-09-23
Trace ID: `KQ-V3.0.3-H07-X-POS-CART-RESPONSIVE`

Status: PASS — perubahan presentation-only.

Perubahan:
- Menyesuaikan `#cartModal` pada desktop agar memanfaatkan ruang viewport secara proporsional.
- Lebar desktop dibatasi nyaman (`min(92vw, 820px)`) dan tinggi dibatasi viewport.
- Mobile tetap menggunakan perilaku modal sebelumnya.
- Struktur DOM POS tidak dirombak.
- `admin/script.js` tidak diubah.
- Tidak ada perubahan Firebase, transaksi, scanner, atau logika keranjang.

Validasi:
- Forced 430px Admin: tetap 0.
- Perubahan file: `admin/style.css` + dokumentasi README FIRST.
- Tujuan: memperbaiki presentasi cart desktop tanpa mengubah mesin POS.


### LOG AKTIVITAS — H-07-Y-R1 — ADMIN NAVBAR POSITION CLEANUP — 2026-09-23
Trace ID: KQ-V3.0.3-H07-Y-R1-ADMIN-NAVBAR-CLEANUP
Status: PASS — koreksi presentation-only setelah uji lokal.

Keputusan layout final:
- Desktop >=768px: navbar/toggle menu berada di ATAS.
- Mobile <768px: navbar/toggle menu tetap di BAWAH.
- FAB TIDAK DIPINDAHKAN oleh breakpoint desktop; posisi dasarnya tetap di samping bawah.
- Pada mobile, posisi FAB tetap berada di atas bottom navbar sesuai aturan existing.

Perubahan:
- Menghapus override desktop khusus `.fab-container` dari H-07-Y yang sebelumnya mengubah `bottom/right`.
- Aturan posisi FAB kembali mengikuti baseline existing.
- Aturan desktop navbar dan floating-menu tetap dipertahankan.
- `admin/index.html` dan `admin/script.js` tidak diubah.
- Firebase/transaksi/scanner tidak disentuh.

Verifikasi:
- Forced 430px Admin: tetap 0.
- FAB desktop: tidak lagi mendapat posisi khusus dari breakpoint.
- FAB mobile: tidak berubah.
- Perubahan file: `admin/style.css` + dokumentasi README FIRST.


### LOG AKTIVITAS — H-07-Z — MODAL & OVERLAY CLEANING — 2026-09-23
- Fokus: membersihkan positioning Floating Menu dari inline HTML tanpa mengubah perilaku.
- `#floating-menu-popup`: `position/bottom/right` dipindahkan ke `admin/style.css`.
- Override desktop H-07-Y tetap menentukan navbar-top placement (`top:70px`, `bottom:auto`).
- Mobile tetap `bottom:70px; right:14px`.
- `admin/script.js`: tidak diubah.
- Firebase/DB: tidak diubah.
- POS/scanner: tidak diubah.
- Status audit: PASS — perubahan presentasi/layout minimum.
- Trace ID: KQ-V3.0.3-H07-Z-MODAL-OVERLAY-CLEANING


### LOG AKTIVITAS — H-07-AA — CSS OVERRIDE AUDIT — 2026-09-23
**Trace ID:** KQ-V3.0.3-H07-AA-CSS-OVERRIDE-AUDIT
**Status:** PASS — AUDIT / NO UNSAFE MASS CLEANUP

**Temuan:**
- `admin/style.css` masih memiliki 387 deklarasi `!important`; tidak dihapus massal karena banyak berada pada layer tema/colorful dan safety overrides yang dapat mengubah tampilan bila dicabut.
- Tidak ditemukan kembali forced viewport `430px` pada `admin/style.css`.
- Selector berulang yang ditemukan tidak diperlakukan sebagai duplikasi otomatis; beberapa berasal dari layer tema/media yang berbeda dan tetap memiliki fungsi cascade.
- Tidak dilakukan perubahan JS, Firebase/DB, POS, scanner, atau struktur navigasi.

**Keputusan cleaning:** hanya override yang terbukti redundant dan aman yang boleh dihapus pada tahap berikutnya. Audit ini sengaja tidak melakukan penghapusan massal agar tidak mengulang pola tambal-sulam.

### LOG AKTIVITAS — H-07-AB — CHAT / DRAWER / AI FLOATING AUDIT — 2026-09-23
**Trace ID:** KQ-V3.0.3-H07-AB-CHAT-DRAWER-AI-AUDIT
**Status:** PASS — AUDIT / NO CODE CHANGE

**Temuan:**
- Chat pribadi menggunakan wrapper relatif dengan drawer absolut; tidak ditemukan ketergantungan kembali pada viewport 430px.
- Drawer daftar pelanggan memiliki lebar responsif dan breakpoint mobile tersendiri (`min(330px, 88%)`).
- Area pesan chat menggunakan overflow internal sehingga drawer/chat tidak perlu memaksa halaman utama bergeser.
- AI Admin menggunakan wrapper `position: fixed` dan fungsi JS yang menghitung posisi berdasarkan viewport aktual (`getBoundingClientRect`, `innerWidth`, `innerHeight`).
- Kotak AI sudah membatasi ukuran terhadap viewport dan menghitung ulang posisi saat dibuka/digeser; tidak ditemukan forced 430px pada jalur ini.
- Tidak ditemukan alasan aman untuk memindahkan logika positioning AI dari JS ke CSS; hal tersebut justru berpotensi mengganggu fitur drag dan penyimpanan posisi.

**Keputusan cleaning:**
- Tidak ada perubahan pada `admin/index.html`, `admin/style.css`, atau `admin/script.js` pada tahap ini.
- Tidak menghapus inline style chat/AI secara massal karena sebagian merupakan state/struktur visual yang terhubung dengan JS.
- Firebase/DB, POS, scanner, navbar, dan FAB tidak disentuh.

**Verifikasi:**
- Forced viewport 430px Admin: tetap 0.
- Chat drawer: responsive dan tidak mengunci layout utama.
- AI floating: tetap viewport-aware dan draggable.
- Status: PASS — tidak diperlukan patch baru.

### LOG AKTIVITAS — H-07-AC — FINAL ADMIN DESKTOP/MOBILE LAYOUT AUDIT — 2026-09-23
Trace ID: KQ-V3.0.3-H07-AC-FINAL-ADMIN-AUDIT
Status: PASS

Ruang lingkup:
- Verifikasi sisa forced desktop 430px pada Admin.
- Verifikasi positioning navbar, FAB, floating menu, modal, POS, scanner, chat/drawer, dan AI.
- Tidak melakukan refactor massal terhadap legacy inline style atau !important.

Hasil:
- admin/index.html: tidak memiliki referensi 430px/desktop-portrait.
- admin/style.css: tidak memiliki referensi 430px/desktop-portrait.
- admin/script.js: tidak memiliki referensi 430px/desktop-portrait.
- Navbar desktop tetap berada di atas; mobile tetap menggunakan bottom navigation.
- FAB tidak dipindahkan oleh breakpoint desktop; tetap berada di area samping bawah sesuai desain.
- Floating menu mengikuti positioning navbar pada desktop dan tetap kompatibel dengan bottom navigation pada mobile.
- POS cart tetap menggunakan modal architecture yang ada; desktop hanya diberi ruang responsive.
- Scanner tetap komponen fullscreen khusus.
- Chat/drawer/AI tidak menunjukkan ketergantungan pada frame 430px.
- Tidak ada perubahan JavaScript/Firebase pada tahap audit ini.

Keputusan cleaning:
Legacy inline styles dan !important yang belum terbukti redundant sengaja tidak dihapus massal untuk menghindari regresi visual/fungsional. Tahap ini dinyatakan sebagai final layout audit; perubahan berikutnya hanya dilakukan jika hasil test lokal menemukan regresi konkret.



### LOG AKTIVITAS — H-07-AD — ADMIN CLEANING FINAL CONSOLIDATION — 2026-09-23
Trace ID: KQ-V3.0.3-H07-AD-ADMIN-CLEANING-FINAL-CONSOLIDATION
Status: PASS — DOCUMENTATION / BASELINE LOCK

Tujuan:
- Mengunci hasil rangkaian cleaning Admin H-07-W sampai H-07-AC sebagai baseline kerja.
- Memisahkan perubahan layout yang diperlukan dari legacy CSS yang sengaja tidak disentuh karena belum terbukti redundant.

Baseline terkunci:
- Admin desktop tidak lagi dipaksa ke viewport 430px.
- Desktop navbar/toggle berada di atas; mobile navbar tetap di bawah.
- FAB tidak berpindah karena breakpoint desktop; mobile tetap berada di atas bottom navbar.
- POS cart tetap menggunakan modal architecture existing dengan ruang desktop yang responsif.
- Scanner tetap fullscreen khusus.
- Chat/drawer/AI tetap viewport-aware dan tidak dipaksa ke frame 430px.
- Firebase/DB dan logic JavaScript tidak menjadi target cleaning.

Keputusan final:
- Tidak dilakukan mass cleanup terhadap inline style atau !important.
- Tidak ada fitur baru pada rangkaian cleaning ini.
- Perubahan lanjutan hanya dilakukan jika ditemukan regresi konkret pada pengujian.

Validasi paket:
- Struktur ZIP dipertahankan dari H-07-AC.
- README FIRST tetap berada di lokasi `README FIRST/readme.md`.
- Baseline source berasal dari H-07-AC yang telah diuji lokal oleh pengguna.


### LOG AKTIVITAS — H-07-AE — CUSTOMER FINAL LAYOUT AUDIT — 2026-09-23
Trace ID: KQ-V3.0.3-H07-AE-CUSTOMER-FINAL-LAYOUT-AUDIT
Status: PASS — AUDIT ONLY

Baseline: H-07-AD Admin Cleaning Final Consolidation.
Scope: audit responsive Customer tanpa perubahan logic/JS/Firebase.

Hasil:
- Customer tidak memiliki forced desktop 430px aktif; aturan desktop mulai 768px menggunakan viewport penuh.
- Bottom navigation dan FAB memiliki aturan responsive desktop/mobile.
- Modal/overlay desktop diarahkan mengikuti viewport, bukan frame Gateway.
- Customer tidak memakai beforeinstallprompt/deferredPrompt aktif.
- Tidak dilakukan penghapusan massal !important/inline-style karena CSS Customer memiliki banyak aturan legacy yang saling bergantung.
- Tidak ada perubahan kode aplikasi pada tahap ini; audit dinyatakan PASS.


### LOG AKTIVITAS — H-07-AF — GATEWAY FINAL AUDIT — 2026-09-23
Trace ID: KQ-V3.0.3-H07-AF-GATEWAY-FINAL-AUDIT
Status: PASS — AUDIT / NO CODE CHANGE

Scope:
- Gateway tetap menggunakan controlled portrait/mobile canvas; tidak dipaksa mengikuti layout desktop Admin/Customer.
- Hotspot Customer dan Admin tetap menuju `./pelanggan/index.html` dan `./admin/index.html`.
- Asset slide Gateway berada di `assets/gateway/`.
- Root manifest tetap satu (`manifest.json`).
- Audit menemukan `sw.js` tidak termasuk dalam baseline ZIP ini; tidak dibuat atau ditambahkan pada tahap ini agar tidak mengubah arsitektur PWA tanpa bukti kebutuhan.
- `beforeinstallprompt` masih berada pada Gateway sebagai entry install PWA.
- Tidak ada perubahan Firebase, database, Customer, Admin, atau routing.

Keputusan:
Gateway dinyatakan LOCKED untuk layout. Tidak dilakukan patch visual/desktop-resize karena perilaku portrait adalah desain yang disengaja.


### LOG AKTIVITAS — H-07-AG — CROSS-ROUTE AUDIT — 2026-09-23
Trace ID: KQ-V3.0.3-H07-AG-CROSS-ROUTE
Status: PASS — NO CODE CHANGE

Audit alur utama:
- Gateway Customer -> ./pelanggan/index.html: PASS
- Gateway Admin -> ./admin/index.html: PASS
- Customer logout -> /: PASS
- Customer session key cust_phone_v13 dihapus saat logout: PASS
- Tidak ditemukan perubahan Firebase/DB pada audit: PASS
- Tidak ditemukan perubahan JS/HTML/CSS pada tahap ini: PASS

Keputusan: route chain Gateway -> Customer -> logout -> Gateway dan Gateway -> Admin dikunci. Tidak ada patch kode pada H-07-AG.


### LOG AKTIVITAS — H-07-AH — PWA / SERVICE WORKER AUDIT — 2026-09-23
Trace ID: KQ-V3.0.3-H07-AH-PWA-AUDIT
Status: PASS

Root Gateway menjadi satu-satunya entry yang memiliki manifest aktif dan beforeinstallprompt. Referensi manifest PWA yang tidak diperlukan pada Admin dibersihkan. Customer tidak memiliki manifest aktif maupun beforeinstallprompt. Tidak ada sw.js/service worker pada baseline ini, sehingga tidak dibuat atau ditambahkan. Root manifest.json tetap satu-satunya manifest. Firebase/database tidak disentuh.


### LOG AKTIVITAS — H-07-AI — FINAL CROSS-PROJECT CLEANING GATE — 2026-09-23
Trace ID: `KQ-V3.0.3-H07-AI-FINAL-CROSS-CLEANING`
Status: PASS

- Audit root, Gateway, Admin, dan Customer terhadap route/PWA/layout residue.
- Root `manifest.json` tetap satu dan Gateway tetap menjadi entry PWA.
- Gateway tetap memakai canvas portrait dan `beforeinstallprompt`.
- Ditemukan registrasi `/sw.js` di `pelanggan/script.js`, sementara baseline tidak memiliki file `sw.js` dan Customer bukan entry PWA.
- Registrasi Service Worker Customer dihapus sebagai dead/stale registration; tidak ada penggantian Service Worker baru.
- Admin, Gateway, Firebase, dan database tidak diubah.
- Tidak menghapus CSS legacy secara massal.
- ZIP integrity dan syntax check PASS.


### LOG AKTIVITAS — H-08-A — DEAD MIGRATION PLACEHOLDER CLEANING — 2026-09-23
Trace ID: `KQ-V3.0.3-H08-A-DEAD-MIGRATION-CLEANING`
Status: PASS — CLEANUP / NO FUNCTIONAL CHANGE

Scope:
- Audit helper migrasi global -> V13 pada `admin/script.js`.
- Verifikasi apakah helper masih dipanggil oleh runtime Admin/Customer.
- Verifikasi bahwa operational data saat ini sudah melalui `storeCollection()` dan bukan melalui helper migrasi global.

Temuan:
- `migrateGlobalDataToV13()` hanya berisi placeholder disabled dan tidak memiliki pemanggil aktif.
- `v13MigrationRunning` hanya dideklarasikan bersama placeholder tersebut dan tidak memiliki pemakaian aktif.
- Tidak ditemukan pemanggilan helper tersebut di file runtime lain.
- `admin/index.html` memiliki utility maintenance/migrasi tersendiri; itu bukan helper global -> V13 yang diaudit di sini dan tetap dipertahankan.
- `toko_v13`, `ACTIVE_TOKO_ID`, `pengguna`, dan `storeCollection()` bukan target penghapusan karena masih merupakan bagian dari arsitektur tenant V13 aktif.

Perubahan:
- Menghapus deklarasi dead `v13MigrationRunning`.
- Menghapus fungsi placeholder dead `migrateGlobalDataToV13()`.
- Tidak mengubah routing tenant, Firebase/Auth, koleksi operasional, atau data Firestore.

Validasi:
- Referensi `migrateGlobalDataToV13` setelah cleanup: 0.
- Referensi `v13MigrationRunning` setelah cleanup: 0.
- Struktur file tetap 20 file pada paket kerja.
- Tidak ada perubahan pada `README FIRST/readme.md` selain log audit ini.

Keputusan cleaning:
- Dead code terbukti aman dihapus karena tidak memiliki dependency runtime.
- Helper migrasi global tidak diganti dengan mekanisme baru; migrasi data historis tetap berada di luar runtime aplikasi dan tidak dijalankan otomatis.

### LOG AKTIVITAS — H-08-B — LEGACY AUTH & ROUTING RESIDUE AUDIT — 2026-09-23
**Trace ID:** `KQ-V3.0.3-H08-B-LEGACY-AUTH-ROUTING-AUDIT`
**Status:** PASS — AUDIT / NO CODE CHANGE

**Scope:**
- Audit sisa jalur auth lama, routing lama, dan referensi file entry lama setelah split `/pelanggan/` dan penguncian Gateway.
- Memastikan tidak ada file legacy root yang masih menjadi entry aktif.
- Membedakan kode aktif, legacy yang sengaja dipertahankan, dan residue yang belum terbukti aman dihapus.

**Temuan:**
- `pelanggan.html`, `admin.html`, root `style.css`, root `script.js`, `sw.js`, dan `manifest-pelanggan.json` tidak terdapat pada paket baseline kerja; tidak ada entry aktif yang masih menunjuk ke file-file tersebut.
- Gateway aktif menunjuk ke `./pelanggan/index.html` dan `./admin/index.html`.
- Admin menggunakan Firebase Authentication secara aktif; `onAuthStateChanged`, login email/password, dan logout merupakan jalur runtime, sehingga bukan dead code.
- Customer tidak menggunakan Firebase Authentication; login pelanggan tetap menggunakan data koleksi toko dan session key lokal `cust_phone_v13`, sesuai arsitektur Customer V3 saat ini. Kode ini aktif dan tidak dikategorikan sebagai legacy dead code.
- Komentar `EXTRACTED FROM pelanggan.html` pada `pelanggan/script.js` dan `pelanggan/style.css` adalah provenance hasil split. Komentar tidak memengaruhi runtime dan belum dihapus agar audit cleaning tidak bercampur dengan perubahan kosmetik/dokumentasi sumber.
- Fungsi maintenance cache/service-worker pada Customer tetap ada sebagai utility manual. Tidak ada registrasi service worker aktif pada Customer; utility tersebut tidak dikategorikan dead hanya karena service worker tidak menjadi entry aplikasi.
- `toko_v13` dan `ACTIVE_TOKO_ID` tetap dipertahankan sebagai bagian dari tenant V13 aktif.

**Klasifikasi:**
- 🟢 **AKTIF / KEEP:** Firebase Auth Admin, Customer local auth/session, Gateway routing, tenant router `storeCollection()`, utility maintenance yang masih dapat dipanggil.
- 🟡 **LEGACY / INTENTIONALLY RETAINED:** komentar provenance `EXTRACTED FROM pelanggan.html`, identifier/session suffix `_v13`, serta utility yang dipertahankan untuk kompatibilitas/maintenance.
- 🔴 **DEAD / SAFE REMOVE:** tidak ditemukan residue auth/routing yang terbukti dead pada tahap ini.

**Perubahan:**
- Tidak ada perubahan kode.
- Tidak menghapus komentar provenance atau utility maintenance tanpa bukti dependency yang lebih kuat.

**Verifikasi:**
- `node --check` seluruh file JavaScript: PASS.
- File entry legacy root yang diaudit: seluruhnya ABSENT dari paket aktif.
- Tidak ditemukan Firebase Auth call pada Customer.
- Firebase Auth Admin tetap aktif dan lengkap.
- Routing Gateway -> Customer/Admin tetap menggunakan entry folder baru.

**Keputusan cleaning:**
H-08-B ditutup sebagai **PASS / NO CODE CHANGE**. Tidak ada penghapusan tambahan yang aman dan jelas pada auth/routing residue. Tahap cleaning berikutnya sebaiknya berfokus pada residue UI/markup atau duplikasi handler yang dapat dibuktikan tidak memiliki consumer runtime, bukan mengubah arsitektur auth atau tenant.

## H-08-C — Cleaning UI/Markup & Duplicate Handler Residue — PASS
- Audit baseline: H08B Legacy Auth & Routing Audit.
- Checked HTML inline handlers, JS function definitions, DOM ID references, and duplicate IDs.
- Removed only code proven unreferenced by the active source: `resetRiwayat()` and `resetDatabaseBarang()` from Admin, plus `appGoBackIf()` from Customer.
- Preserved the Customer `initDraggableAiFab` IIFE because it is self-invoked and active; static name-count alone must not classify it as dead.
- No duplicate HTML IDs found in Admin or Customer.
- No active handler was removed.
- Firebase/Auth, tenant routing, `toko_v13`, `pengguna`, and operational store routing were not changed.
- Verification: JavaScript syntax check PASS; package structure/integrity PASS.



### LOG AKTIVITAS — H-08-D — CSS DUPLICATE RULE CLEANING — 2026-09-23
**Trace ID:** `KQ-V3.0.3-H08-D-CSS-DUPLICATE-CLEANING`
**Status:** PASS — CLEANUP / NO VISUAL-LOGIC CHANGE

**Scope:**
- Audit duplicate CSS rule blocks pada `admin/style.css` dan `pelanggan/style.css`.
- Hanya menghapus rule yang identik 100% dan muncul ulang tanpa perubahan deklarasi.
- Tidak mengubah cascade/theme rules yang memiliki deklarasi berbeda atau kemungkinan merupakan intentional override.

**Temuan & perubahan:**
- Ditemukan rule identik `[data-theme="colorful"] table tr` + `[data-theme="colorful"] .cart-table tr` muncul dua kali pada masing-masing stylesheet.
- Satu salinan duplikat dihapus dari `admin/style.css`.
- Satu salinan duplikat dihapus dari `pelanggan/style.css`.
- Keyframe percentages dan override theme lain tidak disentuh karena dapat berada pada animation/cascade yang berbeda.

**Validasi:**
- CSS brace balance: PASS.
- JavaScript syntax check: PASS.
- Duplicate exact CSS block yang diaudit: tersisa 1 instance per stylesheet.
- Struktur file tetap 20 file.
- Tidak ada perubahan Firebase/Auth, tenant, routing, HTML markup, atau data Firestore.

**Keputusan cleaning:**
H-08-D ditutup sebagai PASS. Cleaning CSS dilakukan minimal pada duplikasi yang terbukti identik; selector yang tampak berulang tetapi memiliki konteks/cascade berbeda tetap dipertahankan untuk mencegah perubahan visual yang tidak terukur.

### LOG AKTIVITAS — H-08-E — CSS RESIDUE / UNUSED SELECTOR AUDIT — 2026-09-23
**Trace ID:** `KQ-V3.0.3-H08-E-CSS-RESIDUE-AUDIT`
**Status:** PASS — AUDIT / NO CODE CHANGE

**Scope:**
- Audit selector CSS yang berpotensi menjadi residue setelah H-08-D.
- Cross-check selector terhadap HTML, JavaScript, class/id dinamis, scanner markup, chat, inventory, FAB, theme, dan komponen yang dirender runtime.
- Selector tidak dihapus hanya berdasarkan ketidakhadiran pada HTML statis karena sebagian UI dibuat/dikendalikan JavaScript.

**Temuan:**
- Tidak ditemukan selector CSS yang dapat dibuktikan 100% dead dan aman dihapus pada tahap ini.
- Sejumlah selector tampak tidak muncul pada satu sumber statis, tetapi memiliki consumer pada HTML atau markup/runtime area lain; selector tersebut dipertahankan.
- Selector theme (`colorful`, `colorful-v2`, `theme-colorful-v2`), scanner (`reader-*`), inventory, chat, FAB, pagination, dan komponen dinamis dipertahankan karena memiliki konteks runtime atau styling state.
- Token warna hexadecimal yang muncul di selector `[style*="..."]` bukan class/id residue dan tidak dikategorikan sebagai selector mati.
- Tidak dilakukan pembersihan agresif terhadap selector composite/pseudo-state untuk menghindari perubahan cascade visual.

**Klasifikasi:**
- 🟢 **AKTIF / KEEP:** selector yang terhubung ke markup HTML atau komponen runtime.
- 🟡 **POTENSIAL / RETAIN:** selector yang consumer-nya dinamis, conditional, theme-specific, atau berasal dari komponen runtime sehingga belum aman dihapus.
- 🔴 **DEAD / SAFE REMOVE:** tidak ditemukan selector yang memenuhi bukti penghapusan aman.

**Perubahan:**
- Tidak ada perubahan CSS.
- Hanya menambahkan catatan audit H-08-E pada `README FIRST/readme.md`.

**Verifikasi:**
- CSS brace balance: PASS.
- JavaScript syntax check: PASS.
- Cross-check HTML/JS terhadap selector: PASS.
- Struktur file aplikasi tetap 20 file; hanya dokumentasi audit yang diperbarui.

**Keputusan cleaning:**
H-08-E ditutup sebagai **PASS / NO CSS CODE CHANGE**. Tidak ada selector yang cukup terbukti dead untuk dihapus tanpa risiko merusak UI runtime. Cleaning berikutnya sebaiknya beralih ke audit asset/reference residue atau final cross-check setelah rangkaian H-08 selesai.

## H-08-F — Asset & Reference Residue Cleaning
- Audit asset/reference lokal dilakukan terhadap HTML/JS/CSS/manifest.
- Ditemukan 1 referensi asset yang salah path: `admin/index.html` mengarah ke `icon-192.png` relatif ke folder `/admin/`, padahal asset berada di root.
- Diperbaiki menjadi `../icon-192.png`.
- Referensi gambar dinamis seperti `${fotoSrc}`/`${imgUrl}` tidak dihapus karena merupakan runtime values, bukan file path statis.
- Asset gateway (`assets/gateway/*`), icon root, manifest, API, dan stylesheet aktif tetap dipertahankan.
- H-08-F status: PASS.

## H-08-G — HTML Markup & Duplicate Residue Audit — 2026-09-23
**Trace ID:** `KQ-V3.0.3-H08-G-HTML-MARKUP-CLEAN`
**Status:** PASS — AUDIT / NO CODE CHANGE

**Scope:**
- Audit duplicate HTML IDs, hidden/placeholder markup, legacy placeholder text, and inline UI handlers.
- Cross-check static IDs against the relevant runtime JavaScript/inline script blocks.
- Review hidden panels/modals/inputs before classifying them as residue.

**Temuan:**
- Duplicate HTML IDs: **0** pada Admin dan Customer.
- Hidden elements yang ditemukan memiliki fungsi UI/state yang jelas (modal, tab, panel, file input, FAB, scanner, developer/maintenance area) sehingga dipertahankan.
- Placeholder dan empty containers yang ditemukan merupakan input/runtime mount points, bukan dead markup.
- Inline handlers yang tampak tidak ditemukan di external `script.js` ternyata didefinisikan pada inline `<script>` di `admin/index.html`; tidak dihapus.
- Tidak ditemukan duplicate section/markup yang dapat dibuktikan 100% dead dan aman dihapus.

**Perubahan:**
- Tidak ada perubahan HTML/JS.
- Hanya dokumentasi audit H-08-G yang ditambahkan.

**Verifikasi:**
- Duplicate ID check: PASS.
- Inline handler/function cross-check: PASS.
- Struktur aplikasi tetap 20 file; hanya README FIRST yang diperbarui.
- Tidak ada perubahan Firebase/Auth, tenant, routing, atau data Firestore.

**Keputusan cleaning:**
H-08-G ditutup sebagai **PASS / NO CODE CHANGE**. Markup tersembunyi yang memiliki state/consumer runtime dipertahankan agar cleaning tidak merusak modal, tab, form, scanner, atau panel dinamis.


## H-08-H — FINAL CROSS-AUDIT CLEANING — 2026-09-23
**Trace ID:** `KQ-V3.0.3-H08-H-FINAL-CROSS-AUDIT`
**Status:** PASS — H-08 CLOSED / LOCKED

**Cross-audit:**
- Routing Gateway → Customer/Admin: PASS.
- Firebase Auth boundary: PASS; Admin Auth tetap aktif, Customer flow tidak dipaksa menjadi Firebase Auth.
- Firestore operational routing: PASS; tidak ditemukan direct global access untuk `produk`, `pelanggan`, `transaksi`, `catatan`, `chats`, `db_chat_rumpi`, atau `pengaturan` di luar `storeCollection()`.
- Global `pengguna`: tetap dipakai secara sengaja untuk profil/admin identity.
- `toko_v13`: tetap dipertahankan sebagai tenant V13 yang memang aktif.
- Dead migration references (`migrateGlobalDataToV13`, `v13MigrationRunning`): 0.
- Duplicate HTML IDs: 0.
- CSS residue: tidak ada selector yang terbukti 100% dead dan aman dihapus.
- Static asset/reference: manifest root tunggal dan asset icon root ter-resolve; perbaikan path icon Admin dari H-08-F tetap valid.
- Service worker: tidak ada `sw.js` aktif di paket; hanya mekanisme pembersihan registration/cache yang memang merupakan utility runtime.
- JavaScript syntax: PASS.
- ZIP integrity: PASS.

**Perubahan H-08:**
- H-08-A: hapus dead migration state/function.
- H-08-B: audit legacy Auth/routing — no code change.
- H-08-C: hapus 3 dead UI handlers.
- H-08-D: hapus 2 duplicate CSS blocks.
- H-08-E: CSS residue audit — no code change.
- H-08-F: perbaiki 1 static asset path di `admin/index.html`.
- H-08-G: HTML markup audit — no code change.
- H-08-H: final cross-audit — no additional code change.

**Keputusan:** H-08 Cleaning dinyatakan **PASS dan LOCKED**. Tidak ada penghapusan tambahan yang dibenarkan hanya berdasarkan dugaan. Baseline H-08-H menjadi baseline hasil cleaning untuk tahap berikutnya.

## H-09-A — Customer Home & Catalog Functional Audit
- Source baseline: H-08-H FINAL CLEAN LOCKED.
- Customer Home structure verified: store name/info, promo, reorder, recipes, trending, category navigation, catalog view switcher, catalog container.
- Catalog data path verified through `storeCollection("produk")` and tenant `toko_v13`.
- Catalog supports list/grid/card, category filtering, substring search, initial batch rendering, and infinite-scroll loading.
- Customer profile-dependent features remain gated by customer login as designed in V3.0.3.
- Removed one duplicate `<!DOCTYPE html>` declaration from `pelanggan/index.html`; no functional behavior changed.
- JavaScript syntax and package integrity rechecked.
- Status: PASS.


## H-09-C — Customer Product Detail Functional Audit — 2026-09-24
**Trace ID:** `KQ-V3.0.3-H09-C-PRODUCT-DETAIL-AUDIT`
**Status:** PASS — NO CODE CHANGE

**Scope:**
- Audit product-detail modal, open/close history state, dynamic product binding, quantity control, stock handling, add-to-cart, WhatsApp share, and Tanya Admin action.
- Cross-check all runtime callers of `openProductDetail()` from trending, catalog/card/list, inventory, and reorder UI.

**Temuan:**
- `openProductDetail(code)` is consumed by multiple active Customer surfaces; it is not dead code.
- `productDetailModal` exists exactly once in `pelanggan/index.html`.
- Detail fields (`detail-img`, `detail-category`, `detail-name`, `detail-price`, `detail-stock`, `detail-desc`, `detail-qty-input`, `btn-detail-add`) each exist exactly once.
- Quantity logic supports normal units and kilogram increments (`1` vs `0.25`) and respects current stock.
- Out-of-stock state disables the add button; available stock restores the active add state.
- Add-to-cart reuses the existing Customer cart data structure and closes the detail modal after success.
- Browser history state already preserves/restores the product detail modal and detail code.
- Share Product via WhatsApp and Tanya Admin handlers are present in the active modal.

**Perubahan:**
- Tidak ada perubahan HTML/JS/CSS. Detail Product sudah merupakan fungsi aktif pada baseline H-09-A.

**Verifikasi:**
- `openProductDetail()` runtime callers: PASS.
- Detail modal/field uniqueness: PASS.
- Customer `script.js` syntax: PASS.
- No Firebase/Auth/routing/data architecture changes.

**Keputusan:**
H-09-C ditutup sebagai **PASS / NO CODE CHANGE**. Tidak perlu membangun ulang Detail Produk karena fungsi tersebut sudah aktif dan terhubung dengan katalog. Rebuild JSX ditunda sampai seluruh Customer functional flow selesai dan baseline kembali di-clean/locked.

## H-09-E — Customer Order History & Status Audit — 2026-09-24
**Trace ID:** `KQ-V3.0.3-H09-E-ORDER-HISTORY-AUDIT`
**Status:** PASS — NO CODE CHANGE

**Scope:**
- Audit Customer online-order history, status timeline, tenant routing, ordering/sorting, empty state, and Quick Reorder dependency.

**Temuan:**
- Order history reads from `storeCollection("transaksi")`, therefore from the active V13 tenant path `toko/toko_v13/transaksi`.
- Query is filtered by `customerPhone`, so a Customer only receives orders associated with the current logged-in phone number.
- Orders are sorted by `waktuTimestamp` descending, with newest orders shown first.
- Empty history state is handled and clears the Quick Reorder dataset.
- `statusPesanan` has a safe fallback to `Menunggu Diproses`.
- Timeline maps active status into Menunggu → Dikemas → Dikirim → Selesai without changing stored transaction data.
- Transaction item details and total are rendered from the stored transaction snapshot.
- Quick Reorder consumes the same verified `lastFetchedOrders` dataset and resolves product codes against the active tenant product store.
- Reorder is therefore not reading the historical global transaction/product collections.

**Perubahan:**
- Tidak ada perubahan HTML/JS/CSS. Existing order-history implementation is active and correctly routed.

**Verifikasi:**
- `storeCollection("transaksi")`: PASS.
- `customerPhone` filter: PASS.
- Newest-first ordering: PASS.
- Status/timeline rendering: PASS.
- Empty-state handling: PASS.
- Quick Reorder dependency: PASS.
- Customer JS syntax: PASS.
- No Firebase/Auth/routing/data architecture changes.

**Keputusan:**
H-09-E ditutup sebagai **PASS / NO CODE CHANGE**. Customer Order History & Status is ready for the next functional module. JSX migration remains deferred until Customer functional completion and final cleaning/lock.

## H-09-F — Customer Chat Admin + Chat Rumpi Functional Audit — 2026-09-24
**Trace ID:** `KQ-V3.0.3-H09-F-CHAT-AUDIT`
**Status:** PASS — NO CODE CHANGE

**Scope:**
- Audit Customer ↔ Admin private chat, Chat Rumpi, realtime listeners, unread badges, read-state reset, tenant routing, send/reply flow, and Admin moderation controls.

**Temuan:**
- Customer private chat uses `storeCollection("chats")`, with the current customer phone as the chat document ID and a `messages` subcollection.
- Customer messages are written with `pengirim: "customer"`; Admin replies use `pengirim: "admin"`.
- Admin private-chat listener reads the same `chats` tenant collection, calculates `unreadAdmin`, and opens the selected customer's `messages` listener in timestamp order.
- Opening a private chat resets `unreadAdmin` to zero; Admin replies increment `unreadCustomer`, and Customer reads/reset logic clears the customer-side unread counter.
- Customer-side realtime listener displays Admin replies and notification state through the existing badge/sound path.
- Chat Rumpi uses `storeCollection("db_chat_rumpi")` on both Customer and Admin sides.
- Customer Rumpi messages carry sender phone/name and timestamp; Admin Rumpi messages use the existing `Admin` sender marker.
- Rumpi has realtime listeners, unread counters, tab-based read/reset behavior, and Admin-side delete/clear controls.
- All operational chat reads/writes remain tenant-routed; no active global `chats` or `db_chat_rumpi` access was found in the audited runtime paths.
- Existing `escapeHtml` usage protects Customer Rumpi rendering and Admin Rumpi rendering. Private chat rendering remains existing application behavior and was not altered because the audit target was routing/functionality, not a security rewrite.

**Perubahan:**
- Tidak ada perubahan HTML/JS/CSS. Existing Chat Admin + Chat Rumpi implementation is active and connected.

**Verifikasi:**
- Customer private-chat listener: PASS.
- Admin private-chat listener/reply path: PASS.
- Customer/Admin unread flow: PASS.
- Chat Rumpi Customer/Admin listener: PASS.
- Tenant routing through `storeCollection()`: PASS.
- Admin Rumpi delete/clear controls: PASS.
- Customer/Admin JavaScript syntax: PASS.
- No Firebase/Auth/routing/data architecture changes.

**Keputusan:**
H-09-F ditutup sebagai **PASS / NO CODE CHANGE**. Chat Admin + Chat Rumpi siap untuk tahap berikutnya. JSX migration remains deferred until Customer functional completion and final cleaning/lock.


## H-09-G — AI Customer PASS
- Audit sumber: H09F Chat PASS baseline.
- `/api/tanya` dipanggil Customer via `POST /api/tanya` dengan payload `prompt`, `daftarProduk`, dan `namaToko`.
- Model aktif: `openai/gpt-oss-20b`; API key tetap server-side melalui `GROQ_API_KEY`.
- `/api/tts` dipakai Customer untuk suara AI; dependency `node-edge-tts` tetap dipertahankan.
- AI UI Customer memiliki input, send, sound toggle, microphone control, loading state, error state, dan output escaping.
- Ditemukan residue teks `[span_n](start_span)/(end_span)` di system prompt `api/tanya.js`; residue tersebut dibersihkan tanpa mengubah isi instruksi AI.
- Header komentar `api/admin-ai.js` yang salah menunjuk `api/tanya.js` dinormalisasi.
- Tidak ada perubahan kontrak endpoint atau struktur Firestore.
- JS syntax: PASS.
- H-09-G: PASS / CLEAN.

## H-09-H — FINAL CUSTOMER CROSS-CLEAN + LOCK — 2026-09-24
**Trace ID:** `KQ-V3.0.3-H09-H-CUSTOMER-FINAL-LOCK`
**Status:** PASS / LOCKED

**Scope:** Final cross-audit seluruh Customer setelah H-09-A s.d. H-09-G, meliputi Home/Catalog, Search, Detail Produk, Cart/Checkout, Order History, Chat Admin/Rumpi, AI Customer, tenant routing, API, HTML/CSS/asset residue, dan dokumentasi.

**Temuan:**
- Struktur Customer aktif tetap `pelanggan/index.html`, `pelanggan/script.js`, dan `pelanggan/style.css` pada baseline V3.0.3.
- Semua JavaScript aktif lolos `node --check`.
- Tidak ditemukan duplicate HTML ID pada Gateway, Admin, maupun Customer.
- CSS brace balance valid pada Admin dan Customer.
- Koleksi operasional Customer tidak memiliki akses langsung ke koleksi global lama; routing tetap melalui `storeCollection()`.
- `toko_v13` tetap dipertahankan sebagai tenant V13 yang sah.
- `pengguna` global tetap dipertahankan sebagai bagian arsitektur identitas/admin.
- Endpoint AI Customer `/api/tanya` dan TTS `/api/tts` tetap tersedia; residue prompt `[span_n]` sudah 0.
- Manifest aktif hanya root `manifest.json`; Gateway tetap menjadi entry install PWA.
- Tidak ada `sw.js` pada baseline dan tidak dibuat/ditambahkan. Referensi `navigator.serviceWorker` yang tersisa hanya utility untuk membersihkan atau memperbarui registration yang mungkin tersimpan di browser, bukan pendaftaran service worker baru.
- Tidak ditemukan residue asset lokal yang belum terselesaikan pada audit sebelumnya.

**Perubahan H-09-H:**
- Tidak ada perubahan runtime Customer. Tahap ini merupakan final cross-audit dan lock.
- README FIRST diperbarui sebagai catatan penutupan H-09 Customer.

**Verifikasi:**
- Customer Home/Catalog: PASS.
- Search: PASS.
- Product Detail: PASS.
- Cart/Checkout: PASS.
- Order History/Status: PASS.
- Chat Admin/Chat Rumpi: PASS.
- AI Customer: PASS.
- Firebase/Firestore tenant routing: PASS.
- API syntax: PASS.
- Customer/Admin/Root JavaScript syntax: PASS.
- Duplicate HTML ID: 0.
- CSS brace balance: PASS.
- Active manifest: root `manifest.json` only.
- Active service worker file: 0.
- ZIP/package integrity: PASS.

**Keputusan:**
H-09 Customer Functional Completion ditutup sebagai **PASS / LOCKED**. Baseline V3.0.3 Customer tidak lagi menerima perubahan fungsional selama tahap rebuild JSX berikutnya, kecuali ditemukan blocker nyata. Migrasi/rebuild JSX ditunda sampai fondasi Customer terkunci seperti yang telah disepakati.
